# 发测 Checklist 骨架（159 · 待确认）

> 状态：草稿。159 指代的环境/版本未确认，下列 Redis / token 项均为 TODO，**未核对真实配置**。
> 阻塞口径（2026-09-11 收敛）：159 环境定义与部署清单路径属外部平台侧，不在交付物清单里，
> 不再作为内部阻塞项挂着。Redis / token / 健康探针统一记「未核对」。
> 补跑条件：主人提供 159 平台链接（配置路径或内部平台入口），由测试运维立即补跑连线验证。

## 0. 前置信息（待填）
- [ ] 159 对应环境：未核对（预发 / 测试 / 灰度未知）
- [ ] 目标版本号 / commit：TODO
- [ ] 部署清单来源路径：外部平台侧，未获取（当前仓库无 yaml）

## 1. 代码与构建
- [ ] package.json scripts 可执行（当前仓库：/package.json）
- [ ] tsconfig 编译通过（/tsconfig.json）
- [ ] 冒烟脚本 verify-room.mts 跑通：`npx tsx verify-room.mts`（会话/房间链路）
- [ ] desktop / web / dist 三端产物一致

## 2. 环境变量（未核对）
- [ ] Redis 连接：Redis URL / Host / Port / DB / 密码 —— 来源未获取
- [ ] 认证 token：签发方、有效期、刷新逻辑 —— 来源未获取
- [ ] 模型服务 endpoint / key（本服务调度用）
- [ ] 知识库服务地址

## 3. 依赖服务（未核对）
- [ ] Redis 可达 + 持久化策略确认（当前仓库无相关配置，需外部确认）
- [ ] 知识库服务健康检查
- [ ] 模型服务连通性

## 4. 启动顺序
- [ ] 1) 依赖服务（Redis / 知识库）→ 2) 算法与推理服务 → 3) web / desktop
- [ ] 启动后健康探针通过 —— 未核对

## 5. 功能用例
- [ ] 群聊消息收发与多智能体路由
- [ ] 记忆读写（见《记忆系统.md》）
- [ ] 会话房间校验（verify-room 覆盖项）

## 6. 回滚与观测
- [ ] 回滚版本 / 命令：TODO
- [ ] 日志与指标位置：TODO

## 7. 已确认事实（唯一）
- Redis：171:6379 db1 —— 仅一条验证记录，知识库服务与 AI服务 侧记录一致。
  **不作为「已核对」结论**（未做连通性、token 校验、健康探针实跑）。

## 8. 结论
- 发测准入：☐ 通过 ☑ 阻塞（阻塞原因：159 定义与 yaml/Redis 配置来源属外部平台侧，未获取；
  Redis/token/健康探针标「未核对」）
