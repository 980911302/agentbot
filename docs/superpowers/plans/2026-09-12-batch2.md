# 第二批实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans (inline). Steps use checkbox syntax.

**Goal:** @ 高亮、时间戳/复制/重试、群 interaction 接线、通知雏形（interaction 系统通知 + 群未读红点）、主人显示名。

**Spec:** `docs/superpowers/specs/2026-09-12-batch2-design.md`

---

### Task 1: 消息细节 + @ 高亮

**Files:** Create `web/src/format.ts`；Modify `web/src/App.tsx`（formatClock 改引用、send 错误路径带 retryText）、`web/src/components/MessageItem.tsx`、`web/src/components/ChatView.tsx`、`web/src/styles.css`、`web/src/types.ts`（DisplayMessage.retryText）

- [ ] format.ts：搬 `formatClock`，App.tsx 改 import
- [ ] types.ts：`DisplayMessage` 加 `retryText?: string`
- [ ] MessageItem：sender header 右侧时间；气泡 hover 复制按钮；`memberNames` 匹配 @名字/@everyone 分段渲染 `.mention`；error + retryText 渲染「重试」
- [ ] ChatView：透传 `memberNames`（群）、`onRetry`
- [ ] App：errorMessage 支持 retryText；send 的 onError/异常路径带 `trimmed`
- [ ] build 过 → Commit `feat(web): 消息时间戳/复制/重试 + @ 提及高亮`

### Task 2: 群 interaction 接线

**Files:** Modify `web/src/api.ts`（RoomHandlers.onAgentEvent + dispatchRoomChunk 处理 `event`）、`web/src/App.tsx`（群流处理 interaction/interaction_closed）

- [ ] api.ts dispatchRoomChunk：`event === 'event'` → `handlers.onAgentEvent?.(data)`
- [ ] App 群 streamRoom handlers 加 onAgentEvent：interaction → setInteractions 去重追加；interaction_closed → 移除
- [ ] build 过 → Commit `feat(web): 群回合的 ask_user 卡片接线`

### Task 3: 通知雏形

**Files:** Create `web/src/notify.ts`；Modify `web/src/App.tsx`（unread 状态、interaction 通知）、`web/src/components/Sidebar.tsx`（红点）、`web/src/styles.css`

- [ ] notify.ts：`ensureNotifyPermission()` + `notifyIfHidden(title, body)`
- [ ] App：interaction 到达（私聊 onEvent + 群 onAgentEvent）→ `notifyIfHidden('「名字」在等你回答', question)`
- [ ] App：`seenRooms` ref + `unread` state；syncWorkspace 首轮只记基线；room.lastMessage.id 变化且非活跃 → unread+1；onSelect 清零
- [ ] Sidebar：ChannelItem 加 `unread?: number`，行尾红点角标
- [ ] build 过 → Commit `feat(web): 通知雏形 —— 等回答系统通知 + 群未读红点`

### Task 4: 主人显示名

**Files:** Modify `web/src/components/SettingsDialog.tsx`、`web/src/App.tsx`、`web/src/api.ts`（streamRoom 加 ownerName）

- [ ] App：`ownerName` state（localStorage 初始化，默认「主人」）+ setter 持久化；user 消息 senderName 用它
- [ ] api.streamRoom body 加 ownerName；App 调用处传
- [ ] SettingsDialog 通用 tab 加「主人显示名」输入行
- [ ] typecheck/test/build → Commit `feat(web): 主人显示名可配置，进群消息与本地发言`

### Task 5: 收尾

- [ ] 全量 typecheck / test / build；`git log` 核对
