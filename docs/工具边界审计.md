# 工具边界审计与限额

更新：2026-09-15。本次覆盖当前完整装配的 25 个工具（原 24 个 + ManageRoomFlow）。限额由 `src/tools/limits.ts` 与各工具的 schema/服务共同执行，不只是提示词约定。未登记在 `TOOL_LIMITS` 的工具使用回退上限 16000/8000。未新增 token 用量、费用或缓存命中率统计。

## 每个工具的预算

输入为序列化 JSON 的字符数，输出为返回文本字符数；不是实际 token 计费值。超限输入拒绝执行，不截断后执行写入。输出截断必须带可见说明。

| 工具 | 输入上限 | 输出上限 | 额外数量/资源边界 |
| --- | ---: | ---: | --- |
| Read | 5000 | 14000 | 默认 200、最多 500 行；正文 ≤12000；每行 2000 字符；扫描 ≤32MiB；offset/column 续读 |
| ListFiles | 5000 | 12000 | 默认 50、最多 100 条；扫描 ≤5000 条目录项/500 个目录 |
| SearchFiles | 6000 | 12000 | 默认 30、最多 50 条；≤100 文件/8MiB，单文件 ≤1MiB；字面量，不运行用户正则 |
| Write | 40000 | 2000 | content ≤32000 字符；结果文件 ≤2MiB；默认新建、显式追加/覆盖 |
| Edit | 40000 | 2000 | old/new 各 ≤16000 字符；文件 ≤2MiB；唯一匹配、可校验 sha256 |
| Shell | 28000 | 10000 | command ≤24000；单次等 ≤60 秒；运行默认 10、最多 30 分钟；≤8 个运行中进程；≤64 条会话、每条缓存 65536 字符 |
| AwaitShell | 5000 | 10000 | 默认增量输出，每页 ≤8000；offset 重读；字面 pattern；stop 终止进程组；只能操作自己创建的进程 |
| ReadToolOutput | 3000 | 14000 | 原文分页默认 8000、最多 12000 UTF-8 字节；搜索最多扫描 256KiB/30 个命中；owner 隔离；日志单条 32MiB、总正文 256MiB、最多 1000 条 |
| WebFetch | 6000 | 14000 | 正文默认 ≤12000；响应 ≤2MiB；20 秒含 DNS/连接/读取；重定向 ≤3 跳；逐跳及实际 DNS 连接校验 |
| WebSearch | 3000 | 8000 | 最多 5 条；标题 ≤200、摘要 ≤600；20 秒/2MiB |
| RecallMemory | 3000 | 10000 | 默认 10、最多 20 条，每条预览 400；full 最多 4 条完整原文；支持 offset |
| update_state | 12000 | 2000 | fact ≤2000；职责 ≤8000；头像 ≤5MiB 且限制扩展名；项目 ≤20、slug ≤80，拒绝路径穿越/隐式串项目 |
| ListSections | 1000 | 8000 | 默认 20、最多 50 条，支持 offset |
| CreateAgent | 10000 | 2000 | name ≤100、职责 ≤8000；每回合最多 6 个；inbox 来源拒绝 |
| ManageRoomFlow | 16000 | 8000 | 未单独登记，走回退上限；start 至少 1 名参与者；控制动作仅协调者 |
| UpdateAgent | 10000 | 2000 | name ≤100、职责 ≤8000；未指定字段保留 |
| CreateChannel | 3000 | 2000 | 1–6 人；每回合最多 1 群；name ≤100 |
| UpdateChannel | 3000 | 2000 | 每次加/减 ≤6 人；最终仍须 1–6 人；成员身份校验 |
| SendToAgent | 16000 | 4000 | 正文 ≤12000；保留传话深度；不向自己或当前群再次广播；不支持的图片明确拒绝 |
| SendToUser | 24000 | 4000 | 文本 ≤20000；附件 ≤50MiB；选项 2–6 且 ID 唯一；交互等待默认 5 分钟 |
| Task | 16000 | 6000 | prompt ≤12000、标题 ≤200；前台 ≤30 秒后转后台；工作 ≤10 分钟；默认并发 8（支持注入）；工人记录 ≤200，满后需复用 |
| CheckSubagent | 1000 | 8000 | 仅创建者可查；默认/最多 20 条列表；详情输出尾部 ≤3000；完整 ID |
| MessageSubagent | 14000 | 2000 | 每条 ≤12000；积压 ≤8 条/32000 字符；仅创建者操作；禁止重复 drive |
| StopSubagent | 1000 | 2000 | 仅创建者操作；取消模型和已登记进程；保留中间记录 |
| TodoWrite | 24000 | 8000 | 总共 ≤100 项，每项 content ≤500；拒绝重复 ID；合并可单项更新 |

## 全链路保护

- 每轮最多 128 次工具调用，单批最多 8 个；成功工具正文总量及调用参数总量各限 256000 字符。输出预算不足以容纳下一个工具的上限时，先拒绝该调用，避免完成副作用后丢失回执。同参数失败 3 次后不再执行。
- 默认最多 32 轮工具执行；剩余 8 轮、16 次工具调用或输入/输出额度低于 64000 字符时，在当次请求末尾提醒优先修改、验证与交付，不反复写入历史。硬配额拒绝后不再空转；同批剩余调用仅补未执行回执。
- 达到轮次/工具总配额后，额外最多一次无工具的阶段交接请求（30 秒截止，正文最多 4000 字符），没有新增修改权限。空/截断/夹带工具/失败/超时的总结改用最近 6 条有界执行记录兜底；取消或抢占不交付迟到总结。轮数为 `max_iterations`，工具总配额为 `tool_limit`，任务均为 incomplete，不冒充完整验收或自动续跑，已执行副作用不会回滚。
- 每一次模型请求前，按消息、工具定义及序列化包装重新估算上下文；默认 60000 总预算中预留 8192 输出和 15% 估算余量。估算不等于精确 tokenizer，未声称覆盖所有模型窗口配置。
- 先缩短旧工具结果和历史长参数，再成组移出较早调用/结果；不修改磁盘历史。固定系统规则和当前用户任务保留，连固定内容都超限时明确失败。
- 不完整的历史工具调用不会以缺少结果的非法形态提交；`finish_reason=length` 的当前批次不执行，最多纠正 2 次。流式 EOF 缺少结束原因、非法 JSON、正文 >64000 字符或总流 >2MiB 均明确失败；非流式响应 ≤512KiB。
- 摘要水位只推进到实际完整送给压缩器的消息批次；相同时间戳一起处理。单个历史批次 >48000 字符时不伪造已覆盖，原文保留。
- 抢占/取消后不再启动下一项工具；迟到结果可以回填执行账本，但不能写入旧聊天回合。
- 固定规则与动态记忆分为独立消息；工具定义按名称排序，尽量稳定请求前缀。缓存命中仍取决于具体服务商/模型，未增加供应商专有开关或命中统计。

## 已修复的非数量问题

- Read 的大文件拒绝发生在 offset/limit 之前；现改为有扫描预算的流式读取。
- Read 分页改按实际行号、正文、换行与截断标记计量，取消每行虚扣 80 字符；正文和总输出上限不变。截图对应的原始 1053 行 HTML 只读验证由 12 页降为 6 页，不再因保守预扣消耗大量执行轮次。
- 写网页不得不用长 Shell，且 Shell 将整段命令再回传；新增 Write/Edit，Shell 只回命令摘要和增量日志。
- 工具数组 schema 写成 properties 或 string；已改成 items 并逐项校验，工作台仍兼容合法 JSON 数组字符串。
- Task 的 maxWorkers 参数未传到管理器；现已接线，工人不能递归派工，父级取消会传播。
- 项目记忆归属静默回退；现缺归属/无权限会失败。RecallMemory 补上字面命中回退，避免长记录因相关度门槛而查不到。
- 附件来源与 Shell 工作目录不一致；已在交付白名单中的文件可原地交付，工作区产物复制时不覆盖同名用户文件，路径使用 realpath 校验。
- WebFetch 自动重定向绕过初始检查；现逐跳校验、跨源移除 Authorization，实际连接时校验 DNS 解析地址。拒绝压缩响应（要求 identity），避免未受控解压。
- AwaitShell 文案称 RE2，实际执行 JS RegExp；改为明确的字面量匹配，消除用户正则回溯风险。
- 密钥文件原先没有任何读取边界（OPT-07）：数据目录的 `secrets.json`/`model-config.json`/`room-flows/secret.key` 与项目根 `.env`/`.env.*` 现在被 Read/Write/Edit 拒绝、被 ListFiles/SearchFiles 跳过，Shell 在执行进程前按字面检查拒绝；判定用 realpath，文件软链与数据目录自身是软链都挡得住（`src/tools/sensitive-paths.ts`，回归 `test/sensitive-paths.test.ts`）。这是默认拒绝，不是沙箱：命令里用变量/命令替换/编码拼出来的路径仍可能绕过。

## 验证与剩余边界

测试：`test/tool-limits.test.ts`、`test/context-limits.test.ts`、`test/agent-loop-limits.test.ts`、`test/provider-stream.test.ts`，同时回归已有工具、群投递、停止、抢占、持久化测试。模型请求均用本地假服务/假 Provider；没有用真实付费 API 验证缓存命中。第 32 轮刚完成 Edit 的复现场景验证后续只交接、不再执行工具；提示词调整不保证模型一定选择最优编辑路径。

这些是资源边界，不是操作系统沙箱。Read/Write/Edit/Shell 仍能按产品设计访问本机路径；执行不可信命令仍需独立权限/沙箱。文件原子提交有工具内锁和改动核验，但不保证抵御恶意外外进程在检查与提交之间抢写。Shell 的进程组取消按 POSIX 实现，Windows 仍仅能保证终止直接子进程。搜索超过扫描预算会明确提示缩小路径，不承诺对超大仓库一次搜全；长行超过预览范围时需 Read 按列核对。

Shell 与工人派生的子进程不继承后端进程环境：`src/tools/services/child-environment.ts` 用白名单只放行 PATH/HOME/locale/终端/node 工具链等必要变量，`AGENT_*`、`OPENAI_*` 一律不给——模型密钥（`AGENT_API_KEY`）因此不会进入工具输出、模型上下文或落盘日志。这是密钥边界，不是命令权限边界：智能体仍可执行它有权限执行的本机命令。

未做项：用量/费用/命中率统计、沙箱体系、后台工人归档 UI、图片/PDF 理解、实时注入运行中的工人推理。这些不会被本次修复宣称已完成。
