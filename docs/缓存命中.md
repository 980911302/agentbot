# 缓存命中

整理日期：2026-09-12；实现核对：2026-09-13（见第 10 节）

模型和宿主都不能保证服务端命中。宿主能做的是保持**同一段 token 前缀**稳定，让供应商有机会复用前缀缓存 / KV。变化位置之后不能复用旧前缀，变化位置之前仍可能复用。

下面区分设计建议与 AgentBot 已实现的行为，不声称是其他平台内部拼装的实测结果。模型、最低可缓存长度、有效断点、缓存保留和路由等条件由供应商决定；不同供应商的规则不能一概而论。

---

## 1. 前缀稳定条件（不是命中保证）

在模型和相关请求设置兼容的前提下，后续请求的已渲染前缀需要与已缓存前缀匹配；同一会话不是通用的必要或充分条件：

```
token[0 .. k]  与已缓存前缀相同
```

才可能命中 `0 .. k`。

所以要做的不是「提示词写得聪明」，而是：

1. 最长、最贵的那一段尽量永远相同。
2. 会变的东西尽量靠后、尽量短。
3. 必须变的时候，只改后缀，或只补一份差量，不要重排前面。

AgentBot 不自行实现供应商的 KV 缓存。下文的本地哈希只校验恢复快照是否仍适用，不作为 API 缓存键或命中证据。工具定义和相关请求设置也可能属于服务端渲染前缀，不能只比较 system 的文字。

供应商条件参考：[OpenAI Prompt caching](https://developers.openai.com/api/docs/guides/prompt-caching)。具体参数应在供应商适配层按目标模型核对，本批未新增供应商缓存参数。

---

## 2. 长期设计示意（不是当前全部已实现）

从前往后，越靠前越不该动：

```
system
  ① 基座规矩          出口、语气、工具纪律、停、协作     几乎不变
  ② 身份              名字 / title / 职责               改资料才变
  ③ 同事与群名单      能叫谁、在哪个群                   加人减人才变
  ④ 技能目录          路径 + 何时去读                    摘要刷新时才重排
  ⑤ 记忆切片          画像 + 最近日志 + 共用「关于你」    每轮可能多一条
  ⑥ 例程名单          有哪些定时任务                     增删例程才变
messages
  ⑦ 更早摘要          对话太长时把旧轮收成一段
  ⑧ 最近原文          私聊和群回合同一条线
  ⑨ 本轮标签+用户句   [room "…"]、当前这句话
  ⑩ 工具结果          本轮才有
```

群简报（进群才读的那份技能正文）**不**进 ①。进群是 ⑨ 带房间标签，模型再去读群技能，读出来的内容落在 ⑩ 附近，不污染 ①–④。

技能**正文**不进 system。④ 只是一张目录。用到再 `Read`，结果当工具结果，不影响前缀。

工具目录/懒加载需单独设计；临时增删顶层 JSON schema 本身可能破坏前缀。当前 AgentBot 保留稳定排序的全量已授权工具定义，每个执行循环固定一次，不在本批改成懒加载。

---

## 3. 中途变记忆：差量，不重写

建议：任务已经开跑之后，新增记忆不要原地替换稳定前缀，必要时通过工具结果或普通消息追加更新。以下只是差量更新的概念示例，不是通用 API 字段：

```
instructions_update
  这是相对「已经渲染过的那份」的增减行
  未改的段落继续沿用旧副本
```

新任务或下一次**对话压缩 / 摘要**时再构建新的记忆切片。编辑、删除既有记忆，以及职责或权限变化不能为了缓存而被忽略；恢复时应校验并使不再适用的快照失效。

AgentBot 不要在 `remember` 之后重跑整个 `composeSystem` 再塞回同一棵进行中的树。进行中的树继续用开轮时的 system；新事实写进存储，下一轮或摘要后再编进去。

若必须让当前轮看见刚写下的事实：当工具结果回给模型，或追加一条短 update，不要把 ①–④ 重排。

---

## 4. 对话变长：摘要旧轮，稳住后面

上下文不够时，把 ⑦ 以前的原文收成一份第三人称摘要，⑧ 只留最近原文。

作用有两个：

- 总 token 掉下来。
- 之后每一轮的「摘要 + 最近原文」可以重新变成一段稳定前缀，直到再攒长。

摘要本身会改前缀，所以不要每轮摘要。应在达到上下文硬上限之前留出回答、工具结果和包装的空间，再触发必要的压缩/裁剪。压缩器使用固定提示词；本批保留现有摘要触发策略，并继续使用每次请求前的窗口预算保护。

---

## 5. 哪些会打破命中（按伤害排序）

| 动作 | 伤害 | 做法 |
| --- | --- | --- |
| 基座/身份改措辞、调换段落顺序 | 整段从 ① 作废 | 基座做成常量，禁止运行时拼接日期、随机数、本轮房间名 |
| 工具列表顺序或 description 每轮不同 | 从工具段作废 | 按名字排序，文案进仓库，不要现场生成 |
| 把当前用户句、时间戳、房间名写进 system 头 | 缩短可复用前缀，不一定是零命中 | 这些只进 messages 尾 |
| 每轮把全部记忆、全部技能正文铺进 system | 前缀又长又飘 | 切片 + 懒加载 |
| `remember` 后重写整份 system | 进行中的树前缀断掉 | 差量或等下一轮 |
| 群纪律复制进每个人的 `instructions` | 每个同事一份不同的「基座」 | 群纪律只在进群时读 |
| 日期写进 ① | 日期变化之后的前缀不能复用 | 日期放当前句附近，不要放基座 |

「稳定」指同一配置下不要无故改。日期只在 messages 尾部变化可保留前缀一致，但不代表服务端缓存一定会保留到次日。

---

## 6. AgentBot 怎么接

设计重点是**顺序契约**和**有条件复用**。本批已将 `composeSystem` 限定为基座/身份/职责，记忆交给 `composeMemory`，实际落地顺序见第 10 节。

推荐 `composeSystem` 固定顺序（缺段就跳过，不要把后面的段顶上去填空——空段用固定缺省或直接省略且省略规则固定）：

```
BASE_PROMPT                 // ① 全员同一份常量
identity                    // ② composeIdentity
instructions                // ② 这个人的职责
roster                      // ③ 同事与群，按 id 排序
skillCatalog                // ④ 名称 + 何时用，按 id 排序
portrait                    // ⑤ 画像
shared                      // ⑤ 共用「关于你」
log                         // ⑤ 最近日志，条数上限固定（例如 8）
scratch                     // ⑤ 随手，条数上限固定
// 以下不要进 system：
// brief / 当前任务 / 房间标签 / 用户句 / 工具结果 / 检索全文
```

`brief`（群、同事来信、恢复旧树、例程）进 **messages 里本轮第一条**，或 system 的**最后一块**。选一个，钉死。推荐进 messages，system 才能跨回合复用 ①–⑤。

检索（`recall`）结果当工具结果，不要写回 system 中段。

开一轮时保存 `systemSnapshot`，同一执行的工具循环沿用已组装的消息；明确续跑、插话补跑和重启恢复时，在兼容性检查通过后复用。新任务重新构建；不跨智能体/会话恢复。快照不能恢复旧权限，也不能盖过用户最新要求。

---

## 7. 切片上限（建议，可改数字，不要改「有上限」）

| 段 | 建议 | 超了怎么办 |
| --- | --- | --- |
| 画像 | 固定条数和 token 上限 | 未注入的仍在记忆存储中 |
| 共用「关于你」 | 固定条数和 token 上限 | 只记跨助手的稳定事实 |
| 日志 | 最近 6～10 条，或固定 token 预算 | 旧的只在 `recall` 时出现 |
| 随手 | 固定小条数/预算 | 过期按既有记忆策略处理，不因缓存优化额外删除 |
| 技能目录 | 只留 name + 一句话 when | 正文按路径读 |
| 最近原文 | 按 token 预算从后往前留 | 前面进摘要 |

条数、预算写成常量。不要「看这轮松紧再多塞两条」，那会让 ⑤ 每轮长度漂。

---

## 8. 运行时检查（完成标准）

同一棵未摘要的树里，连续两轮请求：

1. `system` 的 ①–④ 哈希相同。
2. 若没新记忆、没改资料：①–⑤ 也相同。
3. 新用户句只出现在 messages 尾，不出现在 system 头。
4. `remember` 成功后，当前树的 system 哈希不变；下一轮或摘要后才看到新事实进切片。
5. 进群不改 ①–④；房间名只在 messages 的标签里。
6. 工具 schema 的序列化字符串跨轮相同（排序稳定）。

本地单测用哈希检查输入稳定性，不必连接供应商；实际是否命中必须另看供应商返回，不能从本地哈希推断命中率。

---

## 9. 不要写进这份设计的

- 某家 API 的 `cache_control` / 断点 token 数（换模型就废）
- 命中率目标、省了多少钱（没有数）
- 停止砍树、pid、云电脑
- 把 `say` 写回出口

和 `提示词.md` 的分层是同一件事：基座在前、职责在中、回合在后。本文只补「为什么这个顺序不能乱」和「变的时候怎么变」。

## 10. 2026-09-13 本批已实现

- 请求顺序：稳定规则 system → 固定预算记忆 system → 历史摘要 user → 最近原文 → 本轮检索/文件资料 user → 本轮 brief system → 用户当前句。工具调用和结果继续在尾部追加。brief 为独立尾部规则，不属于持久前缀；检索和摘要标记为资料，不提升为 system 指令。
- 默认记忆正文预算：画像 2000、共用 1000、日志 1250、随手 750 个估算 token；按配置 `sections.memory.max` 固定比例分配，不随本轮余量变化。条数继续遵守既有分层策略；同时间戳按 owner/id 排序。
- 系统快照独立存放在数据目录 `tasks/prompts/<taskId>.json`，文件权限 0600，单份最多 256 KiB；与任务进度的停止记录一起裁剪（最多 500 个停止任务，活跃任务不裁）。不在公开任务详情中附带完整提示词。
- 恢复校验包含智能体/会话、规则、模型、项目范围、当前工具 schema、预算、分层策略和摘要版本。新增记忆不重写原快照；已注入记忆被修改、删除或过期后重建。当前工具仍由最新注册表装配。
- 损坏/缺失/旧版本快照安全回退重建，不自动重放工具；新任务不继承旧任务快照。后台工人保留既有独立 prompt/历史，不复用主智能体快照。
- 自动补跑单独保留最新真实用户要求，即使它已滑出最近原文窗口；请求裁剪与只读收尾都保护这条要求。修正了短历史未计消息包装开销而可能丢掉用户句的问题。
- 显式设置工具清单（包括空清单）会持久标记为 explicit，启动不再自动补权；default 策略仍能升级默认工具集。没有策略标记的旧版本已有清单按 explicit 保守保留，新增工具需明确加入该清单。
- `test/prompt-cache.test.ts` 覆盖前缀/预算/排序、记忆更新、有效性边界、落盘重启、实际插话、工具循环和权限撤销；不依赖真实 API。
- 本批未做：工具懒加载、供应商专有断点/缓存参数、缓存命中率或用量统计；不保证具体命中率和费用。
