# 停止与插话

整理日期：2026-09-12  
对象：运行时怎么实现「对话停」和「中途接着说」。  
这是行为契约，不是某份宿主调度源码。

相关文档：`群聊与智能体交互.md`（扇出 / 1:1）、`智能体可操作能力.md`（智能体代劳）、`调整建议.md`（P0 二次叫醒）。

---

## 1. 先分清三件事

| 东西 | 是什么 | 不是什么 |
| --- | --- | --- |
| 停止按钮 | 用户触发「作废令」的一种入口 | 不会自动掐死已经甩出去的活 |
| 对话停 | 一条**停止令**，对整棵任务树作废 | 不是输入框旁那颗键本身 |
| 接着聊 | 新指令插到队首；旧树**不砍** | 不是 409，也不是把上一件偷偷做完再听你 |

停止按钮不代表下级已经停了。能往下传的是停止令。按钮只是触发器。

---

## 2. 和当前 AgentBot 的差别

| | 现在 | 目标 |
| --- | --- | --- |
| 同一智能体忙着又来一句 | `409`，新话进不来 | **立刻开新回合** |
| 没说停 | — | 先接新句；旧活还欠着；能并就并，打架听最新 |
| 说了停 | — | 上一棵树整棵作废，只听这一句 |
| 未回答的选项卡 | — | 用户又说话了就作废，不当答案 |

用户打字过程不可见。只有**发送成功**之后才有新回合。

---

## 3. 总流程

```
用户发出一句
        │
        ▼
本智能体开启新回合（发送后才可见）
        │
        ├─ 像「停 / 取消 / 先别做了」──→ 作废令
        │         停自己的模型流、后台活、PID
        │         按 id 私发下级（紧急插队）
        │         群里只能普通说一句，下一轮才看见
        │         下级若正在跟用户说话：不打断，回合后再停
        │
        └─ 普通接着聊 ──→ 新指令插到队首
                  旧树不砍，只是先听这句
                  能并就并，打架听最新
                  没回的选项卡作废
```

优先级从高到低：

1. **正在进行的用户回合**（谁都不能打断）
2. **停止令 / 紧急私发**（插队，可打断例程和后台，不可打断上面第 1 条）
3. **普通用户新句**（开新回合，旧树挂起仍欠着）
4. **同事普通私发**
5. **群广播、例程**

---

## 4. 三张表

### 4.1 回合 `turn`

| 字段 | 说明 |
| --- | --- |
| `id` | 本回合 |
| `agentId` | 哪个智能体 |
| `source` | `user` / `agent` / `room` / `routine` |
| `text` | 触发正文 |
| `kind` | `normal` / `stop` |
| `treeId` | 指向哪棵任务树 |
| `status` | `running` / `parked` / `done` / `cancelled` |
| `createdAt` | |

认停止词由**运行时**做，不要等模型「想起来去通知别人」。建议词（可配）：`停`、`停止`、`先别做了`、`取消`、`别做了`、`halt`、`cancel`、`stop`。整句是停止，或明显是在叫停当前活。

### 4.2 任务树 `tree`

这一轮派生出去的东西，停的时候按这张表走。

| 字段 | 说明 |
| --- | --- |
| `id` | |
| `rootTurnId` | 种下这棵树的回合 |
| `agentId` | 树根智能体 |
| `jobs[]` | 本地后台：shell pid、SSE、模型流 abort |
| `children[]` | `{ agentId, via: 'dm' \| 'room', roomId? }` |
| `status` | `open` / `cancelling` / `cancelled` |

「接着聊」**不**改 `tree.status`。只有停止令才把它标成 `cancelling`。

### 4.3 收件箱（已有，补一种信）

在现有 `InboxItem` 上增加：

| 字段 | 说明 |
| --- | --- |
| `kind` | `message`（默认）/ `stop` |
| `treeId` | 要作废的树；空则作废该智能体当前所有 open 树 |
| `priority` | `stop` 必须 `true`，插到队首 |

`drain` 时：同一批里只要有 `stop`，先处理停止，再考虑要不要看别的普通信。  
正在服务**用户回合**时：停止件排队，本回合结束后立刻 `drain`，不要把用户这句话掐掉。

---

## 5. 停止令怎么执行

不要让模型决定「要不要通知谁」。认到 `kind=stop` 或停止词之后，运行时按序做：

1. 先回用户一句（「在停」），避免以为没听见  
2. 中止本智能体：模型流、本回合 SSE、`tree.jobs` 里的 pid  
3. 对 `tree.children`：  
   - `via=dm` → 收件箱塞 `kind=stop, priority=true`  
   - `via=room` → 只能往群里写普通文本（「先停，别继续」），**不能**标紧急；对方最早下一轮看见  
4. 自己不要再继续、做完、恢复或重派旧活  
5. 树上清空（子节点也回报停完，或超时）再告诉用户停完了  

下级收到 `stop`：对自己的孩子做同一件事。这是递归，不是只停第一层。

已经跑到一半、停令还没送到的，拦不住。停止是「尽快传下去」，不是瞬间全宇宙冻住。

---

## 6. 接着聊怎么执行

用户新句**不是**停止词时：

1. 若本智能体正在跑旧回合：把旧 `turn` 标 `parked`，旧 `tree` 保持 `open`  
2. 开新 `turn`，`kind=normal`，**新树**只记录这句新派出去的活（不要把旧孩子挂到新树上，否则停新句会误杀旧活——或反过来）  
3. 先执行新句  
4. 新句结束后：若旧树仍 `open` 且没被停，把欠着的旧活接回去（能并进新目标就并；目标冲突则丢弃旧目标，只保留旧树里还在跑、且不和用户新意冲突的副作用，或直接问一句还要不要）  
5. 未回答的选项卡：本回合作废，不当作用户选择  

同一智能体允许「一棵 open 旧树 + 一个 running 新回合」。不要再用单锁 `409` 把新话挡在门外。锁要从「整个智能体只能一回合」改成「模型调用可以打断/切换，用户回合优先」。

---

## 7. 谁能被打断

| 正在干什么 | 用户新句（非停） | 停止令（紧急私发） | 群里一句「停」 |
| --- | --- | --- | --- |
| 空闲 / 例程 / 后台 | 立刻开新回合 | 立刻作废对应树 | 下一轮才看到 |
| 跟用户说话的回合 | 插队开新回合（旧树挂起） | **等这句用户话结束再停** | 下一轮 |
| 正在处理另一条同事来信 | 用户话优先，同事信回到队列 | 可打断同事信和例程 | 下一轮 |

「用户回合优先于一切同事来信和例程」——所以你正在跟它说话时，别人的停止令先排队，不把这句用户话掐掉。

---

## 8. 派活时必须记账

`send_to_agent` / `post_to_room` / 拉起后台命令 时，运行时写进**当前 turn 的 tree**：

```
tree.children += { agentId, via: 'dm' | 'room' }
tree.jobs    += { pid | abortController }
```

不记账就无法往下传停止令。模型不用、也不该自己维护这张表。

群回合的 `say` 不是派活，只是开口；不要把「被 @ 的人」当成自己的 child（那是扇出，不是你雇的下级）。只有**你主动** `send_to_agent` / `post_to_room` 叫人去干一件事，才挂到树上。

---

## 9. 停止按钮（UI）

输入框旁可以有一颗「停止」：

- 作用：给**当前对话里的这个智能体**发一条等价于停止词的用户回合  
- 不要做成只 `abort()` 本路 SSE  
- 点了之后走第 5 节整条链：回「在停」→ 砍树 → 通知下级 → 报停完  

没有按钮时，用户打「停」必须同样走这条链。

---

## 10. 建议改动点（对照现有代码）

| 位置 | 现在 | 改成 |
| --- | --- | --- |
| `AgentRuntime` 锁 / `isBusy` → 409 | 忙则拒 | 新用户句开新回合；旧 turn parked |
| `http.ts` 群/私聊 SSE | `close` 只 abort 本路 | 若 body 是停止词或客户端发 stop，走砍树 |
| `AgentInbox` | 只有普通信 + priority 插队 | 增加 `kind: 'stop'`，`drain` 优先处理 |
| `send_to_agent` / 将来的 `post_to_room` | 只投递 | 投递同时写入 `tree.children` |
| 群回流 | 无停止语义 | 群文本不标紧急；下一轮 `buildRoomBrief` 若看到停止词再各自停 |

---

## 11. 一句话

停的是**任务树**，不是按钮。  
接着聊是**新回合插队**，旧树还欠着。  
停止令由**运行时**认词、记账、下发；模型只负责回话，不负责去「想起来通知同事」。
