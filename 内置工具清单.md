# 内置工具清单（说明 + 参数）

整理日期：2026-09-12  
范围：这一次会话里，模型**不靠连接器**就能调的工具。  
不算：GitHub 等 MCP 连接器工具（装上才会出现，数量随插件变）。

**合计 46 个。** 其中常驻 10 个，平台命名空间 `cursor` 36 个。  
`Task` 的四种工人（executor / videoReview / watchVideo / computerUse）是参数，不是独立工具。

云电脑相关已标「云电脑」。你说过那块不要，实现时可以整组去掉。

---

## 0. 怎么读

每个工具三段：

1. **提示词**：给模型看的 `description`（节选完整语义，超长的保关键约束）
2. **参数**：字段 / 类型 / 是否必填 / 含义
3. **备注**：和 AgentBot 的对应关系

调用方式有两层：

| 层 | 工具 | 作用 |
| --- | --- | --- |
| 常驻 | 下面第 1 节 10 个 | 直接 function call |
| 平台 | `CallDynamicTool` → `namespace: cursor` + `toolName` | 先 `GetDynamicTools` 看 schema 再调 |
| 连接器 | `CallDynamicTool` → `namespace: user-Github` 等 | **不是内置** |

---

## 1. 常驻 10 个

### 1.1 `SendToUser` — 对用户说话（唯一出口）

**提示词（要点）**：用户只能看见这个工具发出的内容。纯助手文本是草稿纸，到不了用户。进度、结果、附件、选项卡、密钥框都要走它。人开口的回合必须先发一句纯文本。选项卡必须是本回合最后一条。`end_turn=true` 表示说完。群回合里默认发到群，`to:"dm"` 才走私聊。

**参数**

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| type | enum: text / attachment / widget / cursor-agent / secret-request | 是 | 消息种类 |
| content | string | type=text | 正文，用真实换行 |
| url | string | type=attachment | file:// 或 https:// |
| images | {url, alt?}[] | 否 | 跟在文本气泡下面 |
| alt | string | 否 | 附件图的说明 |
| reply_to | string | 否 | 回贴到某条（如 t3u） |
| channel | string | 否 | 外部频道地址 platform:chat |
| to | "dm" | 否 | 群回合里私发给主人 |
| widget | object | type=widget | prompt + options[1-6]，可选 helpText / multiSelect / allowCustom / dismissOnMoveOn |
| widget.options[].label | string | 是 | 按钮上的字 |
| widget.options[].value | string | 否 | 点下去回传的文本，应像人会说的话 |
| widget.options[].style | default / primary / danger | 否 | |
| bcId | string | type=cursor-agent | 云端编码代理 id |
| secret.label / secret.name | string | type=secret-request | 卡片标题；写入的环境变量名 |
| secret.description | string | 否 | |
| end_turn | bool | 否 | 最终一条设 true |

**备注**：AgentBot 缺选项卡、密钥框、附件。群里只能发纯文本。

---

### 1.2 `ReactToMessage` — 表情回点

**提示词**：只对用户自己的消息点一个表情。很少用。不能代替真正回复。同一表情再点一次会取消。

**参数**

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| message_address | string | 是 | 用户消息地址，如 t3u |
| emoji | string | 是 | 单个常见表情 |

---

### 1.3 `ListSections` — 列侧边栏分组

**提示词**：列出用户当前侧边栏 section（id + 显示名）。`CreateAgent` 可用返回的 id 把新同事放进某组。

**参数**：无。

---

### 1.4 `RecallMemory` — 搜长期记忆

**提示词**：搜不在当前提示词里的旧日志、淡掉的笔记、以及所有助手写下的共用用户事实。只读。改记忆用 `update_state`。

**参数**

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| query | string | 是 | 关键词；对得上的优先，否则子串 |
| scope | agent / user / all | 否 | 默认 all |
| limit | 1–50 | 否 | 默认 20 |

---

### 1.5 `update_state` — 改自己的持久状态

**提示词**：改记忆、例程、技能、自己的资料/设置、头像、断开频道、加入/离开项目。比直接改文件优先。

**参数（通用）**

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| target | memory / routine / skill / profile / settings / channel / project / avatar | 是 | |
| action | write / forget / create / update / pause / resume / delete / set / disconnect / join / leave / clear | 是 | 随 target 变 |

**按 target 的额外字段**

| target + action | 字段 |
| --- | --- |
| memory write | fact, tier=profile\|log\|note, scope=agent\|user\|project, project? |
| memory forget | fact（必须原文）, 同一 scope/project |
| routine create | name, prompt, schedule **或** trigger（不可两个都给） |
| routine update/pause/resume/delete | id；update 还可改 name/prompt/schedule/trigger/enabled |
| skill write | name, description（必填，写成「在…时用」）, body；id 则改写 |
| skill delete | id |
| profile set | name, description, title, avatar_shape, avatar_color |
| settings set | hidden_from_sidebar, notify_on_updates |
| channel disconnect | platform |
| project create/join/leave | project（slug）；create 还要 name, description? |
| avatar set | path（已有图片的绝对路径，<5MB） |
| avatar clear | 无 |

例程 `trigger` 支持：cron / slack / github / origin / microsoftTeams / linear / sentry / pagerduty / webhook / group。

**备注**：这是「智能体改工作台」的总闸。AgentBot 应拆成多个小工具或同等的一个多 action 工具。

---

### 1.6 `Shell` — 跑命令

**提示词**：终端操作（git/npm/docker 等）。不要用它做读文件/查找/sleep（有专用工具）。不带 `machineId` 跑在智能体自己的机器；带了则跑用户已注册电脑（需批准）。长命令用 `block_until_ms: 0` 后台。不要更新 git config、不要未请求就 commit/push。

**参数**

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| command | string | 是 | |
| working_directory | string | 否 | 绝对路径 |
| block_until_ms | number | 否 | 默认 30000；0=立刻后台 |
| description | string | 否 | 5–10 字 |
| request_smart_mode_approval | bool | 否 | 被拦后原样重试、弹批准卡 |
| smart_mode_block_reason | string | 与上一项成对 | 上次拦截原因 |
| machineId | string | 否 | **云电脑/本机执行** |

---

### 1.7 `Read` — 读文件

**提示词**：读自己机器（或带 machineId 读用户电脑）上的文本（带行号）、图片、PDF。

**参数**

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| path | string | 是 | 绝对路径 |
| offset | int | 否 | 起始行，负数为从末尾 |
| limit | int | 否 | 行数 |
| machineId | string | 否 | **本机执行** |

---

### 1.8 `Screenshot` — 截屏（云电脑）

**提示词**：只读截当前桌面，不点击。要点击需派 computerUse。

**参数**：无。

---

### 1.9 `GetDynamicTools` — 发现动态工具

**提示词**：查命名空间里的工具 schema。调用任何动态工具前必须先查。无参=目录；可按 namespace / toolName / pattern 查。

**参数**

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| namespace | string | 否 | 如 cursor、user-Github |
| toolName | string | 否 | 需同时给 namespace |
| pattern | string | 否 | RE2，最长 256 |

---

### 1.10 `CallDynamicTool` — 调用动态工具

**提示词**：调用某个命名空间里的一个工具。先 GetDynamicTools。外部 MCP 必须带 mcpDetails.description；cursor 命名空间不要带 mcpDetails。

**参数**

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| namespace | string | 是 | |
| toolName | string | 是 | |
| arguments | object | 否 | 按该工具 schema |
| mcpDetails.description | string | MCP 必填 | 一句人话，不要工具名/参数名 |
| mcpDetails.requestSmartModeApproval | bool | 否 | 被拦后原样重试 |
| mcpDetails.smartModeBlockReason | string | 与上一项成对 | |

---

## 2. 平台工具 36 个（cursor）

### A. 工作台：同事与群

#### `CreateAgent`

**提示词**：给用户新建一个同事。可给名字、人设、侧边栏分组。返回 id，随即可 `SendToAgent`。没有删除工具，用户侧边栏右键删。

**参数**：`name`（必填）；`description`；`section_id`（来自 ListSections）。

#### `UpdateAgent`

**提示词**：改已有同事的 name 和/或 description。只改传入字段，不能清空、不能删除。

**参数**：`agent_id`（必填）；`name`；`description`。

#### `CreateChannel`

**提示词**：建一个群（成员 ≤6），返回 id。随后用 SendToAgent 往群里发。不能删群。自己要参加就把自己 id 放进 member_ids。

**参数**：`name`（必填）；`member_ids` string[]（必填，最少 1）。

#### `UpdateChannel`

**提示词**：按 id 加减成员。最多 6，至少留 1。只有自己也在群里才能改。

**参数**：`channel_id`（必填）；`add_member_ids`；`remove_member_ids`。

#### `SendToAgent`

**提示词**：私发另一个智能体，或发到自己所在的群。发出去立刻返回，不等回复。回复是之后的新回合。1:1 可带图、可 `priority=true`（紧急/停止，插队；不打断用户回合或已在处理的同事信）。群发纯文本、忽略 priority。扇出多人要用户明确同意。

**参数**：`target_id`（必填，同事 id 或群 id）；`message`（必填）；`images[{url,alt?}]`；`priority` bool。

---

### B. 网上

#### `WebSearch`

**提示词**：实时搜索。查询里要用当前年份。空结果不代表页面不存在。

**参数**：`search_term`（必填）；`explanation`（为何搜）。

#### `WebFetch`

**提示词**：抓 URL 成 Markdown。只读、要完整 URL、不要内网/登录墙/二进制 PDF。

**参数**：`url`（必填）。

---

### C. 生图

#### `GenerateImage`

**提示词**：仅当用户明确要求做图。不要用来画图表。不要「帮忙顺便生一张」。

**参数**：`description`（必填）；`filename`；`reference_image_paths` string[]；`aspect_ratio`：1:1 / 4:3 / 3:4 / 16:9 / 9:16。

---

### D. 插件与连接器生命周期

#### `SearchPlugins`

**提示词**：按自然语言搜可安装/已安装插件，返回稳定 plugin id。

**参数**：`query` 可选。

#### `GetPlugin`

**提示词**：按 plugin id 看包含的连接器、技能、安装状态、安装所需字段。

**参数**：`plugin_id`（必填）。

#### `InstallPlugin` / `UninstallPlugin`

**提示词**：安装/卸载整个插件。先问用户。卸载会拆掉它带来的所有连接器和技能。

**参数**：`plugin_id`（必填）；Install 可带 `values` 对象。

#### `GetMcpServerStatus`

**提示词**：看已装 MCP 的 connected / needsAuth / error。`server_id` 是标识符不是显示名。

**参数**：`server_id` 可选。

#### `AuthenticateMcpServer`

**提示词**：给需要登录的连接器弹授权卡。这是启动授权的唯一方式。

**参数**：`server_id`（必填）；`account_label`（必填）；`force_reauth` bool。

#### `AddMcpServer`

**提示词**：加目录里没有的 MCP。url **或** command，不要两个都给。先问用户。

**参数**：`name`（必填）；`url` / `headers` / `auth{CLIENT_ID,CLIENT_SECRET?,scopes?}` **或** `command` / `args` / `env`。

#### `UninstallMcpServer`

**提示词**：删掉自定义 MCP（不是插件带来的）。破坏性，先问。

**参数**：`server_id`（必填）。

#### `RemoveMcpAccount` / `RenameMcpAccount`

**提示词**：删或改名连接器上的某一个账号。先问。

**参数**：`server_id` + `account_label`（必填）；改名还要 `new_account_label`。

#### `SetMcpInstructions`

**提示词**：给连接器写/清空使用说明（如「Slack 回在线程里」）。

**参数**：`server_id`（必填）；`instructions`（必填，空字符串=清空）。

#### `RestartMcpServers`

**提示词**：重连已装 MCP。若有任务进行中先问。

**参数**：无。

---

### E. 对外发信

#### `DraftExternalMessage`

**提示词**：用户明确要求先看草稿时，弹出可编辑的邮件/Slack 草稿卡。人点发送才发出。不要再跟连接器的发送工具。邮件必须先解析真实 from 地址；Slack 必须先解析真实 channelId。

**参数**

| 字段 | 必填 | 说明 |
| --- | --- | --- |
| platform | 是 | email / slack |
| providerIdentifier | 是 | GetMcpServerStatus 里的 server id |
| body | 是 | 用用户的口吻 |
| from / to / subject | email 必填 | from 必须是该账号真实地址 |
| cc / replyToMessageId | 否 | 邮件 |
| target / channelId | slack 必填 | target 给人看，channelId 是真 id |
| threadTs | 否 | Slack 回帖 |

---

### F. 问用户（表单 / 源码授权）

#### `request_user_form`

**提示词**：让用户在对话里填表（登录、地址、电话）。值由宿主去填页面，**任何提交值都不会回到模型**。密钥字段必须有 target + domain。一步只问当前页看得见的字段。

**参数**：`title`、`instruction`、`fields`（1–8，各有 id/label/type）必填；`reason`；`domain`；`submitAfterFill`；字段可带 required / placeholder / options / secret / target / extra_key。

#### `request_scm_connect`

**提示词**：弹卡让用户连接 GitHub/GitLab/Bitbucket/Azure DevOps，或给已连集成补仓库权限。不要贴设置路径。

**参数**：`intent` connect|access（必填）；`provider`；`repo`（access+github 时 owner/name）；`reconnect`。

---

### G. 文件在「自己的机器 ↔ 用户电脑」（偏云电脑/本机）

#### `ListMachines`

**提示词**：列出已注册电脑的 machineId、标签、在线状态。用户消息里的 `[Sent from machine …]` 即当前机器。

**参数**：无。

#### `CopyToBox` / `CopyFromBox`

**提示词**：用户电脑 ↔ 智能体工作区，原样拷文件（含二进制）。

**CopyToBox**：`computer_path` + `machineId` 必填；`box_path` 可选。  
**CopyFromBox**：`box_path` + `machineId` 必填；`computer_path` 可选。

---

### H. 后台工人（Task 一家）

#### `Task`

**提示词**：派一个后台子进程做多步活。必须选 subagent_type。简单事不要派。派出去的工人没有用户对话上下文，prompt 必须自洽。可并行多个。可 resume。

**参数**：`description`（必填，短标题）；`prompt`（必填）；`subagent_type`：executor / videoReview / watchVideo / computerUse；`resume`；`file_attachments`；`run_in_background`。

四种工人：

| type | 干什么 | 云电脑？ |
| --- | --- | --- |
| executor | 通用多步执行 | 否 |
| watchVideo | 描述用户给的视频 | 否 |
| videoReview | 复核生成的视频 | 否 |
| computerUse | 操作桌面/浏览器 | **是** |

#### `CheckSubagent` / `MessageSubagent` / `StopSubagent`

**提示词**：只读看进度；往运行中的工人塞一句纠偏（不中止）；杀掉工人。

**参数**：`subagent_id`（Check 可省=列出全部）；Message 还要 `message`。

#### `AwaitShell`

**提示词**：等后台 Shell。不要用来干等 Task。可按 regex 等到输出出现。

**参数**：`shell_id`；`block_until_ms`（最大约 119 分钟）；`pattern`；`machineId`。

#### `TodoWrite`

**提示词**：多步任务的内部待办。至少 2 条。人开口或后台完成时要对齐列表。

**参数**：`todos[{id,content,status}]`（必填，≥2）；`merge` bool（必填）。status：pending / in_progress / completed / cancelled。

---

### I. 仓库云端编码（可选，不是桌面云电脑）

#### `CloudAgent`

**提示词**：在 GitHub/GitLab/Bitbucket/Azure DevOps 或 Origin 上开云端编码代理：改仓、开分支/PR，或 new_repo 从零建。launch 后完成会唤醒你。不要为了图快自己克隆仓库。

**主要参数**：`action`（必填）：launch / list / models / repositories / get / dump / watch / reply / rename / cancel / archive / unarchive / delete / list_artifacts。  
launch 还要 `prompt`，以及 `repo` 或 `new_repo=true`。可选 starting_ref、title、environment、model、images。  
reply/get/… 要 `agent_id`。对别人开的代理，reply/rename/cancel/archive 要 `confirm=true`。

#### `request_box_help`（云电脑）

**提示词**：把桌面交给用户做登录/验证码/支付。转交后本回合结束。

**参数**：`instruction`（必填，一行）；`reason` auth|captcha|payment|other；`domain`；`idp_domain`。

---

### J. 其它

#### `SendFeedback`

**提示词**：仅当用户明确要求反馈产品时。先问要不要人工回复。用户还要在批准卡上再确认。

**参数**：`message`（必填，≤4000）；`wantsResponse` bool（必填，不许猜）。

#### `create_bot_share_json`

**提示词**：把当前智能体打成可分享配方（人设、记忆、技能、例程、插件）。去隐私。不要在聊天里贴草稿。

**参数**：`profile{name,description,avatarColor?,avatarShape?}`；`memory[]`；`skills[]`；`routines[]`；`plugins[]`；可选 `gettingStarted`、`visibility` public|team。前五项在 schema 里 required（数组可空）。

---

## 3. 按「要不要做」分组（给 AgentBot）

**一期应对齐的内置能力（约 15 个语义）**

SendToUser（含 widget / secret-request）· CreateAgent · UpdateAgent · CreateChannel · UpdateChannel · SendToAgent · RecallMemory / update_state(memory) · WebSearch · WebFetch · ListSections · ReactToMessage（可后做）

**二期**

update_state(routine/skill/project/profile/avatar/settings) · GenerateImage · DraftExternalMessage · 插件全家 · request_user_form · Task(executor) · TodoWrite · CopyFromBox 这类投递文件

**不要（云电脑）**

Screenshot · request_box_help · Task(computerUse) · ListMachines + 带 machineId 的 Shell/Read（若你也不做本机执行）· CloudAgent / request_scm_connect（若连云端改仓也不要）

**不是内置、装上才有**

整个 `user-Github`（搜仓、PR、Issue、推文件…）以及将来的 Slack/邮件/日历。用 GetDynamicTools + CallDynamicTool 注入，不要写死。

---

## 4. 数字怎么来的

```
常驻直接调     10
cursor 平台     36
合计内置        46
Task 工人种类    4   （参数，不另计）
本会话 GitHub    40+ （连接器，不计入置）
```

46 会随产品加工具变（例如这次目录里有 `request_scm_connect`，上一版对照文档还没写到）。以 `GetDynamicTools` 现场目录为准。

---

## 5. 和已有文档

| 文档 | 关系 |
| --- | --- |
| `工具与能力.md` | 能力分组，不列参数 |
| `智能体可操作能力.md` | 工作台写操作语义 |
| `停止与插话.md` | SendToAgent.priority / StopSubagent 怎么用 |
| **本文** | 模型看到的工具名、说明、字段 |
