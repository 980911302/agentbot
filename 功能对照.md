# 功能对照：平台能力 → AgentBot

整理日期：2026-09-11  
范围：**除云电脑以外**都要。云电脑 = 智能体自己的远程桌面 / 浏览器 / 注册本机执行 / 更新或重置那台电脑。本文不列入。

对照对象：当前 AgentBot 源码与界面（创建智能体 / 创建群、记忆抽屉、成员表、主题等）。

标记：

| 标记 | 意思 |
| --- | --- |
| 有 | 产品里已经能用 |
| 半 | API 或雏形有，界面或行为不完整 |
| 无 | 规格里要，代码里还没有 |

---

## 1. 智能体本身（你点名要的那类）

| 能力 | 平台怎么做 | AgentBot | 要补什么 |
| --- | --- | --- | --- |
| 新建智能体 | 侧边栏加号 / 命令面板 | **半** | 已有对话框：名字 + 职责芯片 + 系统配色。缺：自定义职责长文、头像、标题 |
| 改名字 | 聊天顶栏点名字 → 齿轮 → 名称 | **半** | `PATCH /api/agents/:id` 已支持 `name`，界面没有「智能体设置」页 |
| 改头像 | 同上，设图片 | **无** | 现在是颜色圆 + 生成脸。要：上传图 / 选图，存文件，头像组件读图 |
| 改标题 / 简介 | 齿轮里 title、description | **无** | 只有 `instructions`（职责）。应拆：显示名、一句话简介、系统职责 |
| 改职责 / 人设 | 简介 + 内部指令 | **半** | 创建时可选芯片；创建后不能在 UI 改 `instructions` |
| 配色 | — | **有** | 系统分配 `color`，头像在用 |
| 删除智能体 | 侧边栏该行 **右键 → Delete**，确认后连对话一起永久删。不在设置里，没有归档 | **半** | `DELETE /api/agents/:id` 有（连带消息和记忆）。侧边栏没有右键删除 |
| 隐藏侧边栏 | 仍能聊、仍跑任务，从 Hidden / 搜索进 | **无** | |
| 侧边栏分组 | 自定义 section，新建时可放进某组 | **无** | 现在群和智能体平铺 |
| 每助手通知开关 | 智能体设置里 | **无** | 全局设置也没有通知音 |

**最小产品补齐（对上你举的例子）**

1. 聊天顶栏点名字打开「智能体资料」：头像、名称、标题、简介、职责、通知、隐藏  
2. 侧边栏 `＋` 保持：新建智能体 / 新建群  
3. 侧边栏行右键：重命名、改头像、删除（确认）  
4. 头像：先允许改 `color` + 上传本地图；没有图时继续用现在的生成脸  

---

## 2. 群聊

| 能力 | 平台怎么做 | AgentBot | 要补什么 |
| --- | --- | --- | --- |
| 新建群 | 名字 + 成员（≤ 6） | **有** | CreateDialog 已有 |
| 成员表 | 信息栏 Members | **有** | 抽屉「成员」，可拉人 / 踢人 |
| 改群名 | | **半** | `PATCH` 有 `name`，界面不一定暴露 |
| 解散群 | 用户从侧边栏删 | **半** | `DELETE /api/rooms/:id` 有，侧边栏右键可能没有 |
| `@名字` / `@everyone` | 点谁谁必须开口 | **有** | 输入框已支持；二次叫醒见 `调整建议.md` |
| 沉默提示 | | **有** | 「看过，没开口」 |
| 群内互 `@` 再叫醒 | | **无** | P0，见调整建议 |
| 智能体拉人进群 | 运行时改成员表 | **半** | 用户 UI 有；智能体侧没有「拉人/建群」工具 |

群本身不要做成会思考的机器人——这条已经对。

---

## 3. 对话与协作

| 能力 | 平台怎么做 | AgentBot | 要补什么 |
| --- | --- | --- | --- |
| 1:1 私聊 | 点智能体行 | **有** | |
| 智能体 ↔ 智能体私发 | 异步、可排队、可标紧急 | **半** | `send_to_agent` + inbox 有；用户界面几乎看不到传话线程 |
| 统一对话线 | 私聊+群聊在该智能体一条历史上 | **有** | |
| 流式输出 / 工具卡片 | | **有** | SSE + ToolCallCard |
| 附件（图/文件）进私聊 | | **无** | 群继续纯文本；私聊可先做图片 |
| 选项卡片（widget） | 让用户点选而不是打字 | **无** | 创建智能体、确认删除可用这个 |
| 表单收集（地址/密钥等） | 加密输入，不进对话原文 | **无** | API Key、主人名适合走设置，不要让用户往聊天里贴密钥 |
| Markdown / 代码块 | | **有** | |

---

## 4. 记忆（已有规格，功能层再补齐 UI）

| 能力 | AgentBot | 要补什么 |
| --- | --- | --- |
| 三层 × 三作用域 | **有** | 抽屉可看、可手写、可改层 |
| 「记住这个」进画像 | **半** | 工具 + 抽取有；界面没有「钉成画像」的明确入口（PATCH tier 有） |
| 项目笔记 | **半** | 存储有，没有「项目」一等对象和选择器，写入还落到 `projectIds[0]` |
| 主人资料（名字、时区） | **半** | 共用 `user` 作用域有；设置里没有主人显示名（现在群里还写死） |

---

## 5. 定时与外部事件（平台有，AgentBot 还没有）

云电脑不要，**定时任务和外部监听要**。这是「你不在也干活」的部分。

| 能力 | 说明 | AgentBot |
| --- | --- | --- |
| 定时例程 | cron / 每天 / 每 N 分钟，到期叫醒某个智能体执行一段说明 | **无** |
| 暂停 / 恢复 / 删除例程 | 信息栏列出该智能体的例程 | **无** |
| 事件例程 | Slack / GitHub / 线性任务 / 告警等匹配后叫醒 | **无**（可第二期，先做 cron） |
| 技能（可复用说明书） | 多步骤做法存成技能，任何智能体都能按说明跑 | **无** |
| 工具市场 | 给智能体装卸工具 | **半** | 设置里能看工具数量；没有按智能体勾选 `toolNames` 的 UI |

信息栏结构可直接抄平台（去掉电脑预览）：

```
顶栏点名字
  ├─ 资料（头像 / 名称 / 标题 / 简介 / 职责）
  ├─ 例程列表
  ├─ 通道（外部聊天接入，可后做）
  └─ 群：成员表
```

---

## 6. 连接外部服务（要，但不绑云电脑）

| 能力 | 说明 | 建议阶段 |
| --- | --- | --- |
| 连接器 / 插件 | 接 Slack、邮件、GitHub、日历等，在应用内点授权，不要让用户把 token 贴进聊天 | 二期 |
| 外部频道收发 | 外面群里的人 @ 某个智能体，回复打回那个频道 | 二期 |
| 代发消息 | 以用户身份回 Slack / 邮件，先草稿再确认 | 二期 |
| 模型 Key / 端点 | 设置页填写，不进源码 | **一期（P0）** 现在写死在 `config.ts` |

一期不要做完整插件市场；先把「设置里配模型」和「每个智能体勾选工具」做掉。

---

## 7. 应用壳（不要 Computer 页）

平台设置里和电脑相关的整页不要。剩下这些要：

| 项 | AgentBot | 说明 |
| --- | --- | --- |
| 主题 跟随系统 / 浅 / 深 | **有** | |
| 强调色 | **无** | 可后做 |
| 语言 | **半** | 写死「简体中文」，没有切换 |
| 时区 | **无** | 例程会用到 |
| 通知声 | **无** | |
| 用量 | **半** | 有「用量与运行」页雏形 |
| 主人显示名 | **无** | 群发言人目前写死 |
| 账号登录 | 可后做 | 本地单用户可先不做云账号 |

删除智能体不要放进设置，放侧边栏右键。

---

## 8. 明确不做（云电脑）

不要做、文档里也不要承诺：

- 智能体自己的远程桌面 / 浏览器
- 「更新 / 重置电脑」
- 注册用户本机当执行环境
- 屏幕预览当信息栏第一块（你现在的「屏幕」是工具调用回放，那是另一回事，**可以留**）

现在抽屉里的「屏幕」= 本轮工具调用过程，不是云电脑，保留。

---

## 9. 建议实现顺序（功能，不是修 bug）

和 `调整建议.md` 的 P0 修复并行，功能按这块铺：

**一期（对上你举的例子）**

1. 智能体资料页：头像（色 + 图）、名称、标题、简介、职责  
2. 侧边栏右键：改名、删智能体、删群（确认）  
3. 设置：主人显示名、模型 API Key（去掉源码默认值）  
4. 新建智能体：自定义职责文本，不只芯片  
5. 信息栏在群里继续成员表；在私聊里继续记忆 + 资料入口  

**二期**

6. 例程（先 cron，信息栏能增删停）  
7. 按智能体勾选工具  
8. 侧边栏分组、隐藏  
9. 私聊附件、选项卡片  
10. 项目作为一等对象  

**三期**

11. 技能库  
12. 连接器 + 外部频道  
13. 事件例程  

P0 行为修复仍按 `调整建议.md`：群内 `@` 二次叫醒、清 Key、主人名。

---

## 10. 一句话

你要的不是再造一台云电脑，而是：**每个智能体能被创建、改头像和名字、放进群、被 `@`、自己记笔记；以后还能定时跑、接外部工具。**  
AgentBot 已经有群和记忆的骨架；缺的是「智能体作为可编辑对象」的那一层 UI，再加上例程。
