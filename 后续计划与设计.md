# AgentBot 后续计划与设计

整理日期：2026-09-12  
依据：当前源码 + `功能对照.md` / `智能体可操作能力.md` / `工具与能力.md` / `停止与插话.md` / `内置工具清单.md`

**云电脑整块不做**（远程桌面、浏览器代点、注册本机当执行环境、Screenshot / computerUse / request_box_help）。

群回合继续用 `say` / `stay_silent`（未点名收尾文本不算发言）——这是你们的设计，不要改成平台那套「开口就是打字」。

---

## 0. 现在走到哪

### 0.1 已落地（不要再当缺口）

| 块 | 现状 |
| --- | --- |
| 记忆 | 三层 × 三作用域、去重数字守卫、压缩、抽取；项目写入要显式 `projectId` |
| 群 | 扇出、长名 `@`、`summon` 二次叫醒、成员拉踢、沉默一等公民 |
| 手动 UI | 加号建智能体/群、记忆抽屉、成员表、主题、侧边栏删除、主人名、`@` 高亮、未读、等回答通知 |
| 智能体代劳 | `list_workspace` `create_agent` `update_agent` `update_self` `create_room` `update_room` `post_to_room` `send_to_agent` |
| 一期工具 | `ask_user` `request_secret` `list_secrets` `web_search` `web_fetch` `deliver_file` `remember` `recall` |
| 停止 | 停止词、收件箱 `stop`/`stop-ack`、用户新句抢占（不再 409）、砍树下发、群停止下一轮；停止按钮已摘，只留打「停」 |
| 工程 | 单测、git；明文 API Key 与写死姓名已去掉 |

### 0.2 半成品（先收尾，再开二期）

资料页、分组/隐藏、工具装卸、项目对象、1:1 传话可见性。数据字段大多已经有了，缺的是界面和一条产品线。

### 0.3 还没做

例程、技能、插件/连接器、生图、草稿卡、执行器、导出模板、时区、语言。

---

## 1. 总顺序

```
A 半成品收尾（小、不引入新子系统）
    1 资料页补齐
    2 侧边栏分组 + 隐藏
    3 按智能体装卸工具
    4 项目做成一等对象
    5 1:1 传话让用户看得见
B 二期（新子系统，可并行开例程与技能）
    6 例程（先 cron）
    7 技能库
    8 时区 + 语言
    9 生图
   10 邮件/Slack 草稿卡（有连接器再接线，没有先做卡）
C 三期
   11 插件 / MCP / 授权
   12 后台执行器
   13 导出模板
```

每一项先设计、再改码、再补测试。不要把 C 的插件模型提前塞进 A。

---

## 2. A. 半成品收尾

### A1. 资料页补齐

**现状**：`BotProfileDialog` 只改 `name` / `instructions` / `color`。`AgentRecord` 已有 `title` `description` `avatar` `hidden` `section`。

**目标**：顶栏点名字打开资料，能改：

- 显示名 `name`
- 一行简介 `title`（侧边栏用）
- 长简介 `description`（给人看）
- 职责 `instructions`（给模型看，和 description 分开）
- 颜色 + 头像（一期继续 emoji/短字符；上传图片可放 A1 末或 B）
- 隐藏侧边栏 `hidden`
- （通知开关若还没有字段：先只做「有更新时通知」布尔，存在 settings 或 record 上）

**设计**

- PATCH `/api/agents/:id` 已能改这些字段就只补前端；缺哪个补哪个，**合并写入**，空字符串不当清空。
- 智能体侧已有 `update_agent` / `update_self`，资料页与工具写同一张表。
- 删除仍只在侧边栏右键，不放资料页。
- 头像图：先 `avatar` 字符串；真图用 `.agentbot/avatars/<id>` + record 里 `avatarPath`，`BotAvatar` 有路径走图、没有走脸。不要为了图去上云电脑。

**完成标准**：改完刷新侧边栏立刻变；`update_self` 改的和资料页改的是同一份。

---

### A2. 侧边栏分组 + 隐藏

**现状**：`section` / `hidden` 在 registry；`list_workspace` 会滤 hidden；前端 `filter(!bot.hidden)`。没有分组 UI。

**目标**

- 侧边栏按 `section` 分组，未分组一栏。
- 资料页或右键能「移到分组 / 新建分组 / 隐藏」。
- 隐藏后：侧边栏消失，搜索或「已隐藏」入口仍能进对话；例程和收件箱照跑。

**设计**

- 分组先当字符串标签，不必先做独立 `sections.json`。`CreateAgent.section_id` 那套等真要拖拽再升级成有 id 的表。
- `list_workspace` 已列分组名，继续用。
- 隐藏不是删除：消息、记忆、群籍都留。

**完成标准**：藏起来的同事不占主列表；取消隐藏即回。

---

### A3. 按智能体装卸工具

**现状**：`toolNames[]`，启动时还会把新工具补进所有人。没有 UI。

**目标**：资料页「工具」多选：计算器、文件、联网、工作台、记忆……群回合的 `say` / `stay_silent` **不算可卸**，由运行时按回合注入。

**设计**

- 工具分 `always`（say 等回合工具）、`optional`（计算器/网/工作台）。
- 默认：新建同事装全部 optional，和现在 ensure 行为一致；用户卸掉的不要再被 ensure 加回去——**改 ensureDefaultAgent：只给「从未配置过」的人补全**，或加 `toolsPinned: true`。
- 卸掉 `create_agent` 的同事就不能在对话里建人，这是预期。
- 联网用现有 `config.web` 做全局闸，再叠加每人格的 `toolNames`。

**完成标准**：卸掉 `web_search` 后该同事的 schema 里没有它；重启不会被补回来。

---

### A4. 项目做成一等对象

**现状**：`projectIds` + `memory/projects/<id>.json`，写入已禁止猜 `[0]`。没有项目列表、没有加入/退出 UI。

**目标**

- 项目 = `{ id, name, description }`，存在 `.agentbot/projects/index.json`。
- 智能体 `projectIds` 是参加哪些项目。
- 工具：`create_project` / `join_project` / `leave_project`（退出≠删项目）。
- 记忆面板能按项目桶看；写入 project 必须带已参加的 `projectId`。

**设计**

- 不要用项目名当 id（「白泽」种子现在是字符串，迁一次：种子改成 slug，旧文件兼容映射）。
- 删项目是人做的（设置里），智能体不能删。
- 群不是项目。群是广播；项目是笔记空间。

**完成标准**：两个同事加入同一项目后，两边记忆面板能看到同一份项目笔记。

---

### A5. 1:1 传话让用户看得见

**现状**：`send_to_agent` + inbox 异步。用户几乎只在各自私聊/群里偶尔看到。

**目标**：传话发生时，用户能看见「谁 → 谁：一句话」，并能点进某一方的对话。

**设计（选简单的）**

- 不新开「传话频道」实体。
- 投递时往**双方**的消息流各写一条 `source:'agent'`、`speaker` 为对方名字（你们 drain 已往接收方写 user 消息）。
- 发送方自己的对话也 append 一条 assistant/tool 可见记录：「已私发给某某」。
- 前端：`source==='agent'` 用另一条气泡样式（「来自同事」）。
- 不要把 1:1 内容镜像进群。

**完成标准**：A 私发 B，打开 A 或 B 的窗口都能看到这句，且不是群时间线。

---

## 3. B. 二期

### B6. 例程（先 cron）

**为什么现在做**：资料/隐藏收完后，信息栏需要「例程列表」这一栏。

**数据**（`.agentbot/routines/<id>.json` + `index.json`）

```
id, name, agentId, prompt, schedule, enabled, lastRunAt, lastStatus
```

一期只做 5 段 cron（用户本地时区，见 B8）。事件源（Slack/GitHub）放到 C11 之后。

**运行**

- 进程内每分钟扫一次 due（或 node cron）。
- due → 对该 `agentId` 开 `source:'routine'` 回合，`prompt` 当用户句。
- 占用：用户回合优先；例程遇上忙碌就下一分钟再试，不要 409 丢。
- 停止令：例程回合也挂在任务树上；用户对这个智能体说「停」要能砍掉正在跑的例程回合。

**工具**（运行时认，不要让模型手写 cron 解析器）

- `create_routine` name + prompt + schedule + 可选 agentId（默认自己）
- `update_routine` / `pause_routine` / `resume_routine` / `delete_routine`

**UI**：资料/信息栏下列表：下次运行、暂停、删除。创建可用对话（智能体调工具）或简单表单。

**完成标准**：设「每 5 分钟探 xx」，到点该同事自己开跑，停它对正在跑的那次生效。

---

### B7. 技能库

**数据**：`.agentbot/skills/<slug>/SKILL.md`（frontmatter: name, description）。

**语义**：技能是说明书，**没有触发器**。有触发器的是例程。例程的 prompt 可以写「按某技能做」。

**工具**：`write_skill` / `delete_skill`；系统技能只读（若以后内置发测清单）。

**装进上下文**：`ContextBuilder` 加一段「可用技能」：只放 name+description；模型要全文再 `read_file` 该 SKILL.md。不要把全部 body 塞进每一轮。

**UI**：设置或资料里列出技能；对话里「收成技能」走工具。

**完成标准**：写下「发测 checklist」后，任意同事下一轮能在系统段看到这条技能并按它做。

---

### B8. 时区 + 语言

**为什么卡在例程旁**：cron 必须有时区，否则「每天 9 点」是错的。

**设计**

- 设置里：`timezone`（IANA，默认检测或 `Asia/Shanghai`）、`language`（先 `zh-Hans` / `en`）。
- 写入 `.agentbot/settings.json`（和主人名、模型放一起）。
- 例程调度用这个时区。
- 语言：界面文案表；模型侧在 system 里写「用用户语言回」。不要为语言分两套工具。

---

### B9. 生图

**工具**：`generate_image`。仅当用户明确说做图。参数：description、aspect_ratio、可选参考图路径。

**实现**：走已有 LLM 供应商的图像接口，或单独 `AGENT_IMAGE_*` 环境变量。文件落到工作区，再用 `deliver_file` 或聊天附件发出。也可给 `update_self` 当头像。

**不要**：用它画表格；群里只发「图已生成 + 路径」，图走私聊或附件。

---

### B10. 草稿卡（邮件 / Slack）

**前提**：C11 接上对应连接器之前，卡可以先画，发送按钮显示「未连接」。

**设计**（对齐平台 DraftExternalMessage）

- 工具 `draft_external_message`：platform + body + 路由字段。
- 前端一张可改卡片：To / Subject / Body，人点发送才调连接器。
- 智能体**不要**在出卡后再自己 send。
- 路由必须是解析出来的真地址/频道 id，不许猜。

没连接器时：卡片能复制正文，发送置灰 + 提示去装连接器。

---

## 4. C. 三期

### C11. 插件 / MCP / 授权

**不要为 Slack/GitHub 各写死一套工具。** 做成：

1. 市场目录（或先本地 `plugins.json`）
2. 安装 → 出现一个 MCP namespace
3. `GetDynamicTools` 式：回合组装时把该同事启用的连接器工具 merge 进 schema
4. 授权：设置里弹浏览器/设备码，**密钥进 SecretStore**，不进对话、不进记忆
5. 状态：connected / needsAuth / error

智能体工具只要：`search_plugins` `install_plugin` `uninstall_plugin`（先问 `ask_user`）。授权不要模型经手 token。

GitHub 你在平台那边已经连过——AgentBot 里仍要从零接自己的安装流程。

---

### C12. 后台执行器

**和 CreateAgent 不是一回事**：执行器是一次性工人，不占侧边栏，干完汇报。

**设计**

- `Task` 只开 `executor`（不要 computerUse）。
- 工人有完整工具（文件/网/记忆只读？建议：能用文件和网，**不能** `create_agent`，避免工人再开工人）。
- 记入当前任务树 `jobs[]`：用户说停要杀掉工人。
- 前端：一条「正在后台查 xx」状态，完成把摘要写回对话。

---

### C13. 导出模板

把人设、技能、例程（去隐私）打成一份 JSON/目录，可再导入。最后做。依赖 B6/B7。记忆只导出「约定」不导出私聊原文。

---

## 5. 明确不做

| 项 | 原因 |
| --- | --- |
| 云电脑整组 | 已定 |
| `delete_agent` / `delete_room` 给模型 | 只有人右键 |
| 停止按钮 | 已摘；停止令继续打「停」往下传树 |
| 改掉 `say` / `stay_silent` | 你们的群纪律，保留 |
| 例程还没做就接 Slack 事件 | 先 cron |
| 把群当成项目或记忆库 | 群只广播 |

---

## 6. 建议排期（按依赖）

**第一周（A）**  
A1 资料页 → A2 分组隐藏 → A3 工具装卸 → A4 项目 → A5 传话可见。每天一项，都能单独合并。

**第二周（B 前半）**  
B8 时区（例程前置）→ B6 例程 → B7 技能。

**第三周（B 后半 + C 预研）**  
B9 生图 → B10 草稿卡壳 → C11 设计 MCP 表（先不接真服务）。

**更后**  
C11 接第一个连接器（建议 GitHub 只读）→ C12 执行器 → C13 导出。

---

## 7. 每项开工检查单

做任何一项时：

1. 先改/补对应小节的数据模型（不要先画一堆 UI）
2. 工具与 HTTP 写同一条服务（`Workbench` / 新 `RoutineService`），禁止 UI 和模型各写各的
3. 派活必须 `registerChild` / `registerJob`（停止契约）
4. 破坏性操作先 `ask_user`
5. 单测：至少一条「写了能读到」+ 一条「停/权限拒绝」
6. 改完回写本文：把该项从「计划」标成「已落地」，并改 `功能对照.md` 的有/半/无

---

## 8. 一句话

一期工作台和停止树已经能用。接下来先把资料/分组/工具/项目/传话这五个半成品收干净，再开例程和技能；插件和执行器最后做。云电脑始终不做。
