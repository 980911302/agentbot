# 平台工具与能力清单

整理日期：2026-09-11  
这是「智能体手里实际能调用什么」的总表，不是用户点按钮的那一层。  
云电脑（远程桌面 / 浏览器代点 / 注册本机当执行环境）整块划掉，文末单列「不做」。

和已有文档的关系：

| 文档 | 写什么 |
| --- | --- |
| `智能体可操作能力.md` | 建同事、拉群、改资料（工作台写操作） |
| `功能对照.md` | 对照 AgentBot 有/半/无 |
| **本文** | 其余工具：网、文件、连接器、生图、例程、委派…… |

AgentBot 现在大约只有：计算器、读写沙箱文件、记忆、群回合 `say` / `stay_silent` / `send_to_agent`。下面大多是缺口。

---

## 1. 跟用户说话（对话通道）

| 能力 | 做什么 |
| --- | --- |
| 发消息 | 纯文本；可带图片/附件 |
| 选项卡片 | 让用户点选，而不是打字（建到哪个群、是否扇出） |
| 表情回点 | 轻量回应，不必再回一句 |
| 密钥框 | API Key 等遮罩输入，值不进对话、智能体也看不到明文 |
| 表单卡 | 地址/登录等多字段；提交后宿主去填，对话里不回放内容 |
| 草稿卡 | 邮件 / Slack 先弹出可改的草稿，人点发送才真正发出 |

群里只能纯文本；卡片、附件、选项进不了群。

---

## 2. 管同事和群（工作台）

详见 `智能体可操作能力.md`。工具层对应：

- 新建同事、改同事名字/简介、改自己资料/头像  
- 列侧边栏分组，新建时可放进某组  
- 建群、加人/减人（自己必须在群里才能改）  
- 私发某个同事（异步，可标紧急）  
- 发到整个群（扇出）  
- 写/忘记忆（自己的 / 共用关于你 / 项目）  
- 隐藏自己、开关「有更新时通知」  
- **没有**删除同事、解散群的工具  

---

## 3. 例程、技能、项目

| 能力 | 做什么 |
| --- | --- |
| 例程 | 创建 / 改 / 暂停 / 恢复 / 删除。触发：cron，或外部事件（Slack / GitHub / 告警…） |
| 技能 | 可复用说明书；写、改、删用户技能；系统技能只读 |
| 项目 | 创建 / 加入 / 退出；项目笔记挂在这上面 |
| 导出智能体模板 | 把人设、技能、例程打成可分享配方（去隐私） |

---

## 4. 网上查东西

| 能力 | 做什么 | 限制 |
| --- | --- | --- |
| 搜索 | 实时检索，带链接摘要 | 不是登录态 |
| 抓页面 | URL → Markdown | 公网、只读、不要二进制 PDF；内网/需登录会失败 |

有连接器时优先走连接器（已登录的 GitHub / Slack），不要用搜索去绕过。

---

## 5. 连接器 / 插件（工具会随安装变多）

这是「能力插件化」：装上之后，智能体多出一整组针对该服务的工具。

| 能力 | 做什么 |
| --- | --- |
| 搜插件市场 | 按自然语言找（Linear、Notion、写 Word…） |
| 看插件详情 | 含哪些连接器、技能、要填哪些设置 |
| 安装 / 卸载插件 | 改用户配置，先问再动手 |
| 给连接器做授权 | 弹出授权卡，人在原处登录 |
| 看连接状态 | 已连 / 要登录 / 出错 |
| 加自定义 MCP | 用户给了 URL 或启动命令时 |
| 卸自定义 MCP / 删某个账号 | 破坏性，先确认 |
| 给连接器写使用说明 | 例如「Slack 一律回在线程里」 |
| 重启连接器 | 卡住或刚授完权 |

**本机这一次已经接上的例子：GitHub。** 接上之后智能体可以直接：搜仓、读文件、列 Issue/PR、评论、建分支、推文件、开/改/合 PR、发 Release 相关查询等。  
Slack / 邮件 / 日历同理：先装插件，再授权，再出现对应工具。不要为每个服务手写死工具。

代发消息：用户说「先给我看草稿」→ 草稿卡；说「直接发」→ 走连接器发送工具。

---

## 6. 生图

用户明确说「做一张图 / 图标 / 界面稿」才生成。  
不要用来画图表（图表用代码出）。可指定比例、参考图。生成后当附件发出，也可拿去当头像。

---

## 7. 文件（分清三处磁盘）

| 位置 | 能力 | AgentBot 对照 |
| --- | --- | --- |
| 智能体自己的工作区 | 读、写、跑命令、装依赖、出表格/PDF | 你们有沙箱 `read_file` / `write_file`，范围更窄 |
| 用户本机 | 列出已注册机器；把文件拷到本机指定路径 / 从本机拷进来 | 你们现在没有「写到用户 Downloads」的正式工具，这次文档是靠本机 shell 放的 |
| 对话附件 | 把文件/图直接丢进聊天给用户看 | 半：工具卡片有，任意附件弱 |

云电脑桌面操作（点击、打字、滚动）属于划掉的范围。  
「把做好的 md 放到用户磁盘」可以留：这是文件投递，不是远程桌面。

---

## 8. 后台干活（委派，不是再创建一个侧边栏同事）

和「CreateAgent 建同事」不是一回事：

| 能力 | 做什么 |
| --- | --- |
| 派一个执行器 | 多步调查、跑命令、查网页，干完汇报；可多个并行 |
| 看进度 / 改口风 / 叫停 | 卡住就纠偏或杀掉 |
| 看视频 | 用户丢进来的视频用描述模型看（自己不能「看」视频字节） |
| 任务队列 | 多条并行工作流的内部待办（用户不一定看见） |

云电脑桌面那种委派不要做。执行器可以留：让一个智能体把重活丢到后台，自己继续跟用户说话。

---

## 9. 仓库里改代码（可选，不是云电脑）

和「智能体远程桌面」不同：这是到 GitHub/GitLab 上开分支、改代码、提 PR。  
用户说过云电脑不要；**这一项要不要，单独决定**。有连接器时也可以只读仓、开 Issue，不一定要上完整「云端编码代理」。

---

## 10. 内置技能（有说明书的业务流程）

不是底层工具，是「遇到这类请求先按说明书走」：

- 日程 / 预约  
- 点外卖、订餐厅、叫车、订机票、购物下单  
- 代写/代发邮件和 Slack（先草稿再确认）  
- 装连接器、无连接器时怎么降级  
- 例程怎么写、技能怎么写  
- 群聊回合规矩、对外频道收发  

AgentBot 可以后期做成 `skills/` 目录，而不是全写进系统提示词。

---

## 11. 其它边角

| 能力 | 说明 |
| --- | --- |
| 产品反馈 | 用户明确说要投诉/建议时，代交一封（先确认） |
| 反应一条消息 | 表情，不刷屏 |
| 本回合任务列表 | 内部编排，可不上界面 |

---

## 12. 明确不做（云电脑）

- 远程桌面点击/打字/滚动  
- 把桌面交给用户去过验证码/支付（那是云电脑手递）  
- 更新/重置那台电脑  
- 在对话里删除同事或解散群  

---

## 13. AgentBot 建议的工具分组（实现用）

**一期（工作台 + 对话）**

`create_agent` · `update_agent` · `update_self` · `create_room` · `update_room` · `post_to_room` · `send_to_agent` · `say` · `stay_silent` · `remember` · `recall` · `ask_user`（选项卡）· `request_secret`

**一期（网 + 文件）**

`web_search` · `web_fetch` · 现有沙箱文件 · `deliver_file`（写到用户指定目录）

**二期**

例程 CRUD · 技能 CRUD · 项目 join/leave · 生图 · 插件安装/授权（MCP 动态工具）· 草稿卡

**三期**

GitHub 等具体连接器工具集（由插件注入，不要写死）· 执行器后台委派 · 导出模板

现有 `calculator` 可留着当演示；真正缺的是工作台那一组，不是再加一把计算器。
