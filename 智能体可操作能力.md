# 智能体可操作能力

整理日期：2026-09-11  
和 `功能对照.md` 的差别：那份混了**用户点界面**和**产品功能**。本文只写第二层——

> 用户在对话里说一句话，**当前这个智能体**去改工作台：建同事、改头像、拉群、写信、定闹钟。  
> 不是用户打开「新建」对话框自己填表。

云电脑（远程桌面 / 浏览器 / 注册本机）除外。

---

## 0. 两层不要混

| | 用户手动 | 智能体代劳 |
| --- | --- | --- |
| 谁动手 | 人点侧边栏 `＋`、填名字、勾成员 | 人说「建一个测试运维，拉进白泽联调」 |
| AgentBot 现在 | **这一层已经有** | **几乎没有**（只有 `remember` / `recall` / 群回合的 `say` / `send_to_agent`） |
| 平台 | 也有（右键删除、资料页齿轮） | 对话里就能做完，做完侧边栏立刻出现 |

两边都要：人可以自己点；也可以让某个智能体去做。智能体**不能删除**同事或解散群——删除仍是人在侧边栏右键确认。

---

## 1. 管同事（你刚才说的「创建智能体」）

对话示例：「给我建一个测试运维，绿头像，职责是盯 159。」

| 动作 | 智能体能做 | 不能做 |
| --- | --- | --- |
| **新建同事** | 指定名字、简介、职责、放进哪个侧边栏分组 | 不能偷偷建一堆没人要的 |
| **改同事资料** | 改别人的名字、简介（合并写入，不会把资料抹空） | |
| **改自己资料** | 名字、简介、头像 | |
| **拉进分组** | 新建时带上分组 id | |
| 删除 / 归档同事 | | **只有用户**侧边栏右键删除 |

实现要点：这是工作台写操作，不是「在对话里假装有个新角色」。建完必须出现在 `agents.json` 和侧边栏，有自己的记忆和对话线。

---

## 2. 管群

对话示例：「建个支付联调群，把测试运维和 AI服务拉进去。」「把知识库服务也拉进白泽联调。」

| 动作 | 智能体能做 | 不能做 |
| --- | --- | --- |
| **新建群** | 名字 + 成员 id 列表（自己要参加就把自己算进去） | 群不能嵌套 |
| **改成员** | 加人 / 减人；只有**自己也在群里**才能改 | 不能改到空 |
| **进群发言** | `@` 点名，或把一条话发到群（全员被叫醒） | |
| 解散群 | | **只有用户**从侧边栏删 |

和 `send_to_agent` 的差别：发到群是广播；私发是 1:1 收件箱。

---

## 3. 管记忆

对话示例：「记住我时区是上海，所有同事都该知道。」「忘掉那条过期的端口。」

| 动作 | 说明 |
| --- | --- |
| 写自己的笔记 | 画像 / 日志 / 随手 |
| 写共用的「关于你」 | 所有智能体都能读 |
| 写项目笔记 | 要带上项目 |
| 删除一条 | 按原文精确忘掉 |

AgentBot 已有 `remember` / `recall`。缺的是：智能体改**别人**不该直接改别人的 self 笔记；共用层和项目层要分清。

---

## 4. 管例程（你不在也跑）

对话示例：「每个工作日早上 9 点让测试运维扫一遍 159。」「先停掉这个，别删。」

| 动作 | 说明 |
| --- | --- |
| 创建 | 名字 + 到期要做什么 + 时间表或外部事件 |
| 改 | 改说明、改点、暂停、恢复 |
| 删 | 一次性的看完就删 |

事件源（Slack / GitHub / 告警）可二期。一期先 cron。

---

## 5. 管技能

对话示例：「把发测 checklist 收成一个技能，以后谁都能按这个跑。」

可复用说明书，没有触发器（有触发器的是例程）。智能体可写、可改、可删用户技能；系统自带的只读。

---

## 6. 管项目

对话示例：「建一个白泽项目，把这四个同事加进去。」

项目 = 一份额外的笔记空间 + 哪些智能体参加。智能体可：创建、加入、退出。退出不等于删除项目。

---

## 7. 传话与协作（已有一部分）

| 动作 | AgentBot | 说明 |
| --- | --- | --- |
| 私发某个同事 | **有** `send_to_agent` | 异步，不等返回 |
| 发到整个群 | **半** | 用户发群有；智能体主动 `post_to_room` 工具没有（只有自己 `say`） |
| `@` 拉同事进当前话题 | **半** | 文本可以写 `@`，二次叫醒未做 |
| 同时问一堆同事 | 要克制 | 会把所有人叫醒，回复打回用户侧，默认先问用户同不同意扇出 |

---

## 8. 问用户 / 收敏感信息

| 动作 | 说明 |
| --- | --- |
| 选项卡片 | 「建到哪个群？」点一下，不要让人打字 |
| 表单 | 地址、电话等结构化字段 |
| 密钥输入 | API Key 走遮罩框，**永不进对话原文、不进记忆明文** |

创建同事、拉群、扇出多人，都属于「智能体先问一句再动手」的场景，用选项卡片。

---

## 9. 对外（二期，仍不是云电脑）

| 动作 | 说明 |
| --- | --- |
| 装连接器 | 在应用里点授权，不要用户把 token 发给智能体 |
| 外部频道收发 | Slack 等，按频道地址回 |
| 代发前先草稿 | 邮件 / 群消息，人确认再发出 |
| 搜网页 / 拉页面 | 查资料用，不替代连接器 |

---

## 10. 建议 AgentBot 加的工具（智能体侧）

用户手动 UI 继续留着。另外给模型一组工作台工具：

| 工具 | 做什么 | 权限注意 |
| --- | --- | --- |
| `create_agent` | 新建同事（name, title, description, instructions, color, avatar?, section?） | 建完返回 id |
| `update_agent` | 改同事名字 / 简介 | 合并，禁止空覆盖 |
| `update_self` | 改自己名字 / 简介 / 头像 | |
| `create_room` | 建群（name, member_ids） | 自己要进群就写入 members |
| `update_room` | 加/减成员、改名 | 调用者必须已是成员 |
| `post_to_room` | 以自己身份往群里发一条（扇出） | 不同于 `say`（`say` 是当前群回合开口） |
| `create_routine` / `pause_routine` / … | 例程 | 创建可弹确认 |
| `write_skill` / `delete_skill` | 技能 | |
| `create_project` / `join_project` / `leave_project` | 项目 | |

**不要给智能体的工具**

- `delete_agent` / `delete_room`（人右键）
- 读同事私聊、读同事记忆
- 把用户密钥写进文件

`say` / `stay_silent` / `send_to_agent` 继续只在对应回合出现。

---

## 11. 和「还有什么功能」的关系

按「智能体代劳」还能做、上面没展开成大节的：

- 改自己是否从侧边栏隐藏、是否通知更新  
- 给某个智能体装卸工具（勾选 `toolNames`）  
- 生成一张图当头像（有生图能力的话）  
- 列出现有同事 / 群 / 分组，避免重名再建一个  

不做：云电脑、替你点本机 GUI、在对话里删除同事。

---

## 12. 一期最小闭环

用户说：「建一个测试运维，绿色，拉进白泽联调。」

当前智能体应：

1. `create_agent` → 得到 id  
2. `update_room` 把 id 加进白泽联调（自己是成员才能改）  
3. 回话：「测试运维已建好并进群。」侧边栏立刻能看见  

这就是和「侧边栏加号填表」不是同一个功能的原因。
