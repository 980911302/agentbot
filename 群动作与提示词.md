# 群动作与提示词编写

整理日期：2026-09-13

群里没有第七种「打招呼工具」。招呼是开口的一种内容。提示词也不写成一张巨文：产品规矩钉在最前，人设只写这个人干什么，回合纪律进群再注入。

宿主安全 / 调度 / 审批原文不能外发。下面是产品层实际怎么绑、怎么写。分层总表见 `提示词.md`，缓存顺序见 `缓存命中.md`。

---

## 1. 六个动作

模型在群回合里只能组合这六下。都不是独立人格技能，是出口和正文约定。

### 1. 干活

工具和私聊同一套，不降级。调用过程、普通助手文本，群都看不见。

先干再决定开不开口。查环境、读文件、算结论，都在草稿纸上。

### 2. 开口

`SendToUser`（`type=text`，不带 `to`）打进**当前群**。

- 一轮最多 3 条
- 每条通常 1～3 句，像人打字
- 只有这次调用算发言；收尾模型文本不是发言
- 最后一条 `end_turn=true`
- 附件、选项卡、密钥框进不了群，要改口或走私话

### 3. 沉默

整轮 0 次进群的 `SendToUser`。合法，不是失败。运行时不要补「在」、不要重试。

没被点名、又没有别人没说过且归自己管的实质内容：就该这样。

### 4. 私话

还是 `SendToUser`，加 `to:"dm"`。只进主人和自己的 1:1，群看不见。

问主人要不要把某事说进群、交附件、弹选项，走这条。

### 5. 点名

写在开口正文里：`@名字` 或 `@everyone`。不是单独工具。

运行时解析后把被点到的人再入队，他们下一趟必须开口。见 `群聊点名.md`。

### 6. 喊外面的人

`SendToAgent`：私发同事，或发到**自己所在的、但不是当前这个群回合**的群。贴出去立刻回，不等回复。

当前群禁止再用 `SendToAgent` 叫醒整组。见 `协作投递.md`。

---

## 2. 招呼不是动作

「你们好」没有对应工具。它只是一条群消息，用上面 2 或 3 处理。

平台默认（我这边实际绑的）：

- 没 `@`：在场都有回合，**默认沉默**
- 被 `@` / `@everyone`：必须开口
- 没点名但有独特实质：可以开口
- 问候堆「我在 / 有活说 / 团队齐了」= 旁白和附和，简报禁止

你要的产品规则不一样，必须写进群简报，不能靠「多一个动作」：

```
面向全群的招呼（你们好 / 大家好，且没有 @）：
每个在场的人必须开口，且只准一句短招呼，然后停。
禁止出方案、禁止 @ 同事、禁止续旧活、禁止当报幕。
有实质工作或被点名，再走原来的必须开口 / 默认可沉默。
```

AgentBot 现在的 `naturalResponder`（只准一个人接招呼）和这条是反的。要全员点头，就删掉「别人不要跟着重复」，改成上面这段。

---

## 3. 提示词怎么编写（平台做法）

原则：

1. **一张提示词只干一件事。** 全员规矩、这个人是谁、这一轮房间里发生了什么，分三层，不要抄三遍。
2. **会变的往后放。** 基座当天字节级稳定，人设少动，简报每轮现拼。为的是缓存，见 `缓存命中.md`。
3. **纪律进简报，不进职责。** 「被 @ 必须回」写在群技能里。测试运维的 `instructions` 只写 159、分支、不改业务仓。
4. **表现写给模型，机制留给运行时。** 不要写「入队 / 租约 / pid / 三波」。写「必须开口 / 可以沉默 / 发出去不等回」。
5. **用出口定义发言，不用形容词。** 「开口」= 调了进群的 `SendToUser`。不要靠「请积极发言」。
6. **禁止项写具体反例。** 「不要只回同意/收到」「不要总结整楼」「不要把招呼升级成分工」比「保持专业」有用。
7. **身份先于职责。** 第一句是「你是「测试运维」（id: …）」，否则它认不出 `@测试运维` 是在点自己。

不要写进提示词：

- 完整工具 JSON（走 function schema）
- 停止令怎么砍树
- 宿主安全原文
- 每个同事一份群纪律副本

---

## 4. 四层各写什么

```
system
  ① 基座     全员同一份常量
  ② 身份     名字、id、title
  ③ 职责     这个人干什么（instructions）
  ④ 本轮简报 进群 / 同事来信 / 例程 / 恢复旧树，四选一或没有
  ⑤ 记忆切片 画像、共用关于你、最近日志
messages
  房间标签 + 当前句 + 最近原文
```

### ① 基座（`BASE_PROMPT`）

只放跨场合都成立的出口和边界。短、稳定、禁止运行时插日期或房间名。

该有：

- 唯一声音是 `SendToUser`；普通文本是草稿
- 私聊：短、先给结果；明确执行才开工；闲聊不要自己延展成任务
- 群里开口用 `SendToUser`；私话 `to:"dm"`；不要 `SendToAgent` 当前群
- `SendToAgent` 按 id，贴出即回
- 发消息不是派工；停自己不等于停同事
- 扇出多人先得到主人明确同意

不该有：被 @ 怎么办、三波、naturalResponder、159 的分支。

### ② 身份

```
你是「{{name}}」（id: {{id}}）。
一句话简介：{{title}}
```

名字必须和群成员表一致，否则 `@` 对不上。

### ③ 职责

只写这个人管什么、不管什么、拿不准问不问。

```
你负责测试环境部署与运维。主责 192.168.31.159。
171 是正式+库，连错会写生产。
不改业务仓代码。
群里没点到你、对方又只是招呼：回一句招呼就停，不要出发布计划。
```

最后一句是为了压过「架构师爱出契约」这种人设。人设越强，越要在职责里写清问候边界。不要在这里复制整份群纪律。

### ④ 群简报（进群才注入）

两块：`ROOM_SKILL`（稳定技能正文）+ `buildRoomBrief`（本轮变量）。

`ROOM_SKILL` 写死的动作合同：

```
这是群回合，先干活再决定是否发言。
SendToUser 默认进当前群；to:"dm" 给主人。
只有 SendToUser 算开口；零次就是沉默。
一轮最多 3 条，每条 1～3 句。
只代表自己，不当报幕，不总结整楼，不贴私聊原文。
正文 @名字 / @everyone 请人接话；被点名必须回。
当前群不要再 SendToAgent。
```

`buildRoomBrief` 只加**这一轮**的事实：

```
房间、在场（名字+id）、谁触发
你是否被点名 / @everyone / 同事再点
（若采用全员招呼规则）这是面向全群的招呼：你必须回一句短招呼，然后停
停止令
这一轮已经公开说过的话
```

本轮是否发言，按状态分行，不要含糊：

| 状态 | 写 |
| --- | --- |
| 被点名 / @everyone | 必须 `SendToUser`。对方只是招呼就只回招呼，不要引出任务。 |
| 面向全群的招呼且你们采用全员点头 | 必须一句短招呼，然后停。禁止 @、禁止方案、禁止旧活。 |
| 没点名、不是群体招呼 | 实质 + 别人没说过 + 归你管，才开口；否则不调用出口。 |

同事来信用另一份 `buildAgentBrief`，不要把 `viaAgent` 塞进群简报。

### ⑤ 记忆切片

画像和最近日志。旧日志去搜。不要把整本记忆铺进 system。

---

## 5. 推荐的群简报（按你要的招呼规则）

把现在的 `naturalResponder` 整段换成下面。技能里删掉「只指定一位自然接话人」。

```
## 群回合
- 这是群聊回合，不是私聊。关闭「先开口」：有工作先做完，再决定是否发言。
- `@` 决定谁必须开口。没 `@` 不等于没人该说话。
- 面向全群的招呼（你们好 / 大家好，且没有点任何人）：每个在场的人必须用 SendToUser 回一句短招呼，然后停。禁止出方案、禁止 @ 同事、禁止续旧活、禁止报「团队齐了」。
- 有明确工作请求时，没被点名的人默认沉默；只有别人还没说过、又归自己管的实质内容才开口。
- SendToUser 默认进当前群；to:"dm" 给主人。附件、卡片、选项进不了群。
- 普通助手文本是草稿。整轮零次 SendToUser 就是沉默。
- 工具和私聊同一套。用自己私聊里的结论，不贴原文，不提主人跟别人的私聊。
- 只代表自己。一轮最多 3 条，每条 1～3 句。被点名必须回。
```

本轮变量示例（主人说「你们好」）：

```
房间：白泽联调
在场同事：架构师（id: …）、测试工程师（id: …）
这一轮由「主人」触发。
没有人点名你。
**这是面向全群的招呼：回一句短招呼，然后停。**
```

被 `@架构师` 出方案时：

```
**有人点名了你，必须开口。**
按对方这句话回答；不要把问候扩成全员分工。
```

---

## 6. 编写检查

写完一段提示词，问四句：

1. 这段换一个同事还成立吗？成立 → 基座或 `ROOM_SKILL`。只对这个人成立 → 职责。
2. 这段下一轮会变吗？会变 → 简报变量，不要写进基座。
3. 模型没有这个工具，写了会不会演戏？（入队、三波、pid）有就删。
4. 人设会不会压过它？会（架构师、助手）→ 职责里再钉一句问候边界。

单测仍只测简报字符串和入队标记，不要靠模型「看起来像在打招呼」。
