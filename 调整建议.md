# AgentBot 调整建议

整理日期：2026-09-11  
实施状态：**已全部落地**（2026-09-11 复核，20 项端到端验证 + 40 项单测通过）  
对照：`记忆系统.md`、`群聊与智能体交互.md`、`docs/设计文档.md`、`docs/链路文档.md`，以及当时源码。

结论先说：**规格已经基本落地**，不必推倒重来。`typecheck` 当时能过。真正要动的是下面这些缺口。

---

## 0. 已经做得对的（先别动）

| 点 | 现状 |
| --- | --- |
| 群是扇出，不是中心 AI | `postToRoom` 全体成员进回合，各自决定说不说 |
| `@` 是强信号不是投递开关 | 没被点名也会进这一轮 |
| 被点名必须开口 | 不给 `stay_silent`；收尾文本可兜底算发言 |
| 未被点名默认闭嘴 | 只有显式 `say` 才进房间 |
| 沉默是一等公民 | `status: 'silent'`，前端轻提示「看过，没开口」 |
| 点名解析 | 长名优先、吃掉已匹配片段；`@everyone` 多别名 |
| 1:1 传话 | 收件箱异步，深度上限 3，不是 RPC |
| 记忆 | 三层 × 三作用域；画像溢出降级成日志，不硬报错 |
| 对话线 | 私聊和群聊同一条消息流，靠 `roomId` / `speaker` 标记 |

---

## 1. 优先修

### 1.1 群里 `@` 同事不会二次叫醒 ✅ 已实现

**现状**

- 只解析**用户入站**那一次的 mention。
- 成员 A 回复里写 `@测试运维` 时：
  - mention 会进房间时间线的 `mentions` 字段；
  - 但 `everyone` 被写死成 `false`（`runtime.ts` 回流处）；
  - **不会**再给被点到的人开一轮；
  - 已经跑过的人不会再进回合；
  - 排在后面的人最多在消息历史里看到这句，简报仍可能是「没有人点名你」，不会变成「必须开口」。

**和规格的差距**

`群聊与智能体交互.md` 写明：群里 `@名字` 是把话题甩给同事的公开交互。当前实现只覆盖「用户点名」，没覆盖「成员点名成员」。

**建议改法**

发言回流后：

1. 对每条 `post` 再跑 `resolveMentions`（含 `@everyone`）。
2. 命中「本轮还没被召唤过、或已回合结束但被新点名」的成员 → 再开一轮（或并入本轮召唤集后补跑）。
3. 注意防环：同一 `roundId` 内同一人被重复 `@` 要有上限；深度/轮次预算写进文档。

**主要改动点**

- `src/server/runtime.ts` → `postToRoom` 回流段
- 必要时抽「召唤队列」：用户入站 + 成员发言 mention 共用

---

### 1.2 源码里写死了 API Key ✅ 已实现

**现状**

`src/config.ts` 存在 `DEFAULT_API_KEY` 明文默认值。

**风险**

密钥进仓库 / 进备份 / 进分享目录都会泄露。应视为已暴露，轮换。

**建议改法**

- 只从环境变量读：`AGENT_API_KEY` / `OPENAI_API_KEY`
- 缺 key 时：启动失败，或桌面端弹设置，**不要**回落到源码常量
- 文档写清如何配置；`.env` 进 `.gitignore`
- 已出现在文件里的那枚 key：当作泄露，去服务商侧作废并换新

---

### 1.3 群发言人名字写死 ✅ 已实现

**现状**

```ts
senderName: options.ownerName ?? 'linlin zhang'
```

HTTP 群发消息路径也没有把 `ownerName` 传进来。

**建议改法**

- 配置或设置页提供「主人显示名」
- `POST /api/rooms/:id/messages` 传入或服务端从配置读取
- 去掉对某个具体姓名的硬编码默认值（可用「主人」之类中性占位，或强制配置）

---

## 2. 建议尽快补

### 2.1 扇出是串行的 ✅ 已实现

`postToRoom` 对成员 `for await` 顺序跑。4 个成员 ≈ 4 次 LLM 串行，群会明显慢。

**建议**

- 未点名且互不依赖的成员可并行
- 被点名的可优先 / 仍串行（若需要先听完前面的发言，再决定）
- 若保留「后发言者能看见先发言者」的语义，并行时要在简报里带上本轮已公开 posts，或接受「同一轮内彼此看不见」并写进文档

### 2.2 项目记忆只写 `projectIds[0]` ✅ 已实现

`remember`、记忆抽取、memory 工具多处用 `projectIds[0]`。智能体挂多个项目时会写错本子。

**建议**

- 写入时要求显式 `projectId`，或按当前任务 / 当前房间上下文解析
- 禁止静默落到 `[0]` 或 `agentId`

### 2.3 没有测试、也没有 git ✅ 已实现

目录当时不是 git 仓库，也没有 `*.test.ts`。

**建议至少覆盖**

| 模块 | 用例方向 |
| --- | --- |
| `mentions.ts` | 长名优先、别名、失败当普通文本 |
| `dedup.ts` | 数字守卫：端口不同不合并；同义改写合并 |
| `postToRoom` 召唤 | 用户 `@`；成员发言后再 `@` 应二次叫醒 |
| 沉默 / 开口 | 被点名无 `say` 但有收尾文本 → spoke；未点名仅收尾文本 → silent |

初始化 git，继续 ignore `.agentbot/`。

### 2.4 杂项 ✅ 已实现

| 项 | 说明 |
| --- | --- |
| `createAgentMessageTool` 的 `selfName` | 传入的是 agent id，且工具内未使用 → 删掉或改成真正的名字 |
| 根目录 `release-checklist-159-draft.md` | 像业务草稿，不宜和产品源码长期混放，挪到别处或删 |
| 文档与实现同步 | 二次叫醒、并行扇出、主人名配置落地后，改 `链路文档.md` / `设计文档.md` 对应节 |

---

## 3. 建议落地顺序

```
1. 清 API Key + 主人显示名配置     （安全 / 正确性，改动小）
2. 群内 @ 二次叫醒                 （和规格对齐，产品行为）
3. 项目记忆 projectId 显式化
4. 扇出并行（带好语义说明）
5. 单测 + git
6. 扫尾杂项
```

若只能选一件行为向修复：做 **1.1 群内 `@` 二次叫醒**。  
若只能选一件安全修复：做 **1.2 清 API Key**。

---

## 4. 一句话对照

| 问题 | 要不要改 | 优先级 |
| --- | --- | --- |
| 成员 `@` 同事不叫醒 | 要 | P0 |
| 明文 API Key | 要 | P0 |
| 主人名写死 | 要 | P0 |
| 扇出串行偏慢 | 建议 | P1 |
| 项目记忆 `[0]` | 要 | P1 |
| 缺测试 / 缺 git | 建议 | P1 |
| 死参数 / 草稿文件 | 顺手 | P2 |
| 扇出模型、三层记忆、沉默纪律 | 不用动 | — |

---

## 5. 刻意不写的

- 具体补丁行号（以你改时的文件为准）
- 向量检索、多用户权限等已划在产品边界外的事
- 是否上云 / 是否迁仓（当时是本地目录，无远端）

改完后把本文对应条目勾掉，并回写 `docs/链路文档.md`。
